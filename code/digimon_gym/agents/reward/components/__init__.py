"""Component implementations for the `reward-profiles` capability.

Each module hosts a small family of related components. The registry
in `..registry.py` imports from these modules and exposes the
`kind -> class` mapping consumed by the YAML profile loader.
"""
