name: Desktop Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-desktop:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            args: '--target aarch64-apple-darwin'
            sidecar_profile: gameplay
          - platform: macos-latest
            args: '--target x86_64-apple-darwin'
            sidecar_profile: gameplay
          - platform: ubuntu-22.04
            args: ''
            sidecar_profile: gameplay
          - platform: windows-latest
            args: ''
            sidecar_profile: gameplay

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies (Linux)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: pip install -r requirements-desktop.txt pyinstaller

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      # Build the Python sidecar binary for this platform
      - name: Build sidecar
        run: ./tools/build-sidecar.sh ${{ matrix.sidecar_profile }}

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Build frontend (desktop mode)
        working-directory: frontend
        run: VITE_BUILD_TARGET=desktop npm run build

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: v__VERSION__
          releaseName: 'Digimon TCG v__VERSION__'
          releaseBody: 'Desktop release. See assets for platform installers.'
          releaseDraft: true
          prerelease: false
          projectPath: src-tauri
          args: ${{ matrix.args }}
