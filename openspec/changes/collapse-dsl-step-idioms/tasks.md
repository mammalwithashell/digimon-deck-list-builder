# Tasks — collapse DSL step idioms

> **Implementation note (verified 2026-06-18, before starting):** the field/zone select steps (`SelectHand`/`SelectTrash`/`SelectOwnPermanent`/`SelectOpponentPermanent`) ALREADY receive the dispatcher's *implicit* `tail` (the rest of the process body) at their execution sites in `code/digimon-engine/src/dsl_cards/step/selections.rs` (e.g. `install_select_hand(..., tail.to_vec(), ...)`), so `select_*` + a following step already works via the process sequence. The 8 existing `then:`-bearing structs (`SelectOwnSources`, `SelectOpponentSources`, `DigiBurst`, `SelectOpponentDpBudget`, `SelectOpponentPlayCostBudget`, `SelectOwnBreedingPermanent`, `SelectUnionArgs`, + 1) carry an EXPLICIT scoped `then` that the dispatcher must compose with the implicit tail. **The subtlety to get right:** when a field/zone select gains an explicit `then`, the explicit tail must run scoped to the selection binding AND compose correctly with the implicit dispatcher tail WITHOUT double-running — read how `SelectOwnSources` (selections.rs:645, compiled.rs:1653) threads its `then` into the install helper + `selection_result` and mirror exactly. This is the cloneable VM's `ResumeFrame::RunTail` data, so keep it closure-free. CompiledStep variants (`SelectOwnPermanent`/`SelectHand`/`SelectTrash`/`SelectOpponentPermanent`) need a new `then: Vec<CompiledStep>` field (`#[serde(default)]` for bincode-pack compat — see [[reference_dsl_substrate_authoring_gotchas]]; build.rs regenerates the pack via the build-dep on digimon-dsl).

## 1. Generalize the `then:` action-tail (do first — lowest risk, highest reach)
- [x] 1.1 Added `#[serde(default)] then: Vec<StepSpec>` to `SelectFieldArgs` (select_own/opponent/any_permanent) and `SelectZoneArgs` (select_hand/trash/reveal/security). Because both arg structs are SHARED, the field is lowered for **all 7** variants that use them (incl. `SelectAnyPermanent` + `SelectReveal`) to avoid a silent-drop footgun and per rule 28 (widen uniformly) — `SelectReveal`'s low-level `then` is orthogonal to and forward-compatible with §2's `reveal_search`. Compiled variants gained `#[serde(default)] then: Vec<CompiledStep>` (bincode-pack compat). Lowered via new `compile_then_tail` helper (mirrors `SelectOwnSources::then`). Dispatcher composes `inner_tail = then ++ tail` via new `compose_then_tail` helper, keeping `selection_result` — `then` runs only on a pick (callback) or, for a non-cost optional decline, scoped to the empty binding (no-op, identical to the `SelectOwnSources` min==0 case); the implicit `tail` runs exactly once. Closure-free (cloneable VM `ResumeFrame::RunTail`). Patched 29 `CompiledStep::Select*` literal constructions across the dsl test files + 1 in `compile.rs` (the replacement-choose desugar).
- [x] 1.2 Behavioral parity tests in `tests/dsl/collapse_then_tail.rs`: `select_trash` + `then:[add_to_hand_from_trash]` (mirrors the implicit-tail form in `phase2b_end_to_end`), `select_own_permanent` + `then:[delete_permanent]`, `select_own_permanent` + `then:[suspend]`. All green; full `dsl` target 775/0.
- [x] 1.3 RL-visibility test `then_tail_inner_selection_stays_rl_visible`: a `select_own_permanent` whose `then` contains a `select_hand` installs a NEW `pending_selection` (with exposed candidate actions) after the first pick resolves — no auto-resolution (rule 17).
- [~] 1.4 SKIP `discard_from_hand` / `recover_from_trash` aliases — the `then:` form reads clearly and adding aliases would re-introduce the sprawl this change removes (the task's own guidance).

## 2. `reveal_search` composite
- [x] 2.1 `add_to_hand_from_reveal` / `trash_from_reveal` / `return_to_deck_from_reveal` now move EVERY card in a `CardList` binding (new `resolved_cards` helper in `step/zone_moves.rs` replaces the `singleton_card` cap), so a bucket bound to a multi-card list adds/trashes/returns all of them — fixes the single-card limitation. (`singleton_card` is retained for the genuinely single-card arms.)
- [x] 2.2 Added `reveal_search { of, count, buckets: [{filter, to: hand|trash|deck, max, optional, prompt}...], remainder: top|bottom }` (`step.rs` spec + serde, `compiled.rs` `CompiledStep::RevealSearch` + helper enums, `compile.rs` lowering). It lowers to a single composite compiled step whose executor (`step/selections.rs` dispatcher) expands it at run time into the EXISTING sequence: `reveal_top_deck` → ONE `install_select_reveal_buckets` over all buckets (cross-bucket de-dup always on) → per-bucket reveal-move (§2.1) → `place_remainder_on_deck`, threading the outer tail. Reuses the existing reveal-bucket selection wholesale → parks/resumes identically; pure-data deterministic expansion (cloneable / reset-and-replay safe). A non-`optional` bucket forces `min = max` (the install advances when candidates exhaust → never soft-locks); `optional` → `min = 0`. **`remainder: choose` is DEFERRED to §3** (it needs §3.1's `StackPosition::Choice` player-elected placement; no migration target uses it). `to: deck` returns picks to the deck bottom.
- [x] 2.3 Corner cases in `tests/dsl/reveal_search.rs`: mandatory add-up-to-N (multi-card add), fewer-than-max (clamp, no soft-lock), short/empty pool (no match → nothing added, remainder returned), all-optional bucket declined via PASS, and two-bucket cross-`no_duplicate` routing to distinct destinations (hand + trash). Remainder ordering is exercised by every test (it resolves the inherited `place_remainder` ordering selection).
- [x] 2.4 Migrated **BT9-092** (Cool Boy — reveal 3, add 1 X-Antibody Digimon + 1 X-Antibody Option, bottom rest) to `reveal_search` as a real parity fixture; all 7 of its behavioral tests stay green (the end-to-end test drives via `auto_resolve`, so the select→bucketed-select flow change is transparent). **EX5-015 intentionally NOT migrated:** its "if you added cards, trash 1 from hand" follow-up needs the named bucket bindings (`pick_a`/`pick_b`) that `reveal_search` deliberately encapsulates — it correctly stays on the lower-level `select_reveal_buckets`. Broader mechanical migration of the ~55 longhand searchers is optional (back-compat keeps the longhand valid; proposal "no rewrite forced").

## 3. Security-placement overhaul
- [x] 3.1 Added `StackPosition::Choice` / `CompiledStackPosition::Choice` (+ `compile_stack_position`). A supporting step's executor installs a binary top/bottom `pending_selection` and re-runs placement at the chosen end THROUGH `run_steps` (so a follow-on ordering selection + the outer tail park correctly); `map_stack_position(Choice)` hard-panics (loud guard — Choice must be resolved before placement). Wired `place_remainder_on_deck { position: choice }` (dispatcher match-guard) and §2's deferred `reveal_search { remainder: choose }`. **[committed `4440592d`]**
- [~] 3.2 `position: choice` behavioral test landed (reveal_search remainder:choose — RL-visible binary pick, returns remainder to the chosen end). **BT25-038 clause A → `position: choice` migration is the remaining piece** (it needs a Choice executor arm on the hand-source `PlaceOnSecurity` variant, mirroring `place_remainder`; the manual `select_effect_choice [top,bottom]` form still works meanwhile).
- [x] 3.3 Consolidated the 7 place-on-security verbs (`place_on_security`, `place_self_at_security`, `place_self_option_at_security`, `place_permanent_on_security`, `…_and_handle_replacement`, `…_bottom_…_and_cancel_replacement`, `…_observed`) into ONE source-polymorphic `place_on_security { source, of, position, face_up, disposition, include_sources }`. `source` is an **untagged** enum (`{ card: … }` hand/trash | `{ permanent: … }` | `self` | `self_option`) — untagged because externally-tagged variants serialize as `Value::Tagged` and break the `typed_body()` `from_value` round-trip for `kind: replacement` bodies (same constraint as `LinkCardsCount`). `disposition` = `SecurityReplacementDisposition { none|cancel|handle|observed }`. **Byte-identical compiled output**: the unified verb lowers to the EXISTING compiled variants + executors (the replacement system is untouched).
- [x] 3.4 Migrated all 23 card users → unified verb (`tools` script + per-card verify); **237 behavioral tests across them pass / 0 fail** (byte-identical). Compile-time validation: a disposition requires `source: permanent`; `include_sources` requires `disposition: observed`; **`disposition: cancel` requires `position: bottom`** — making the bottom-only cancel-via-security tuck EXPLICIT rather than a silent footgun (the "bottom+handle asymmetry" fix; both replacement cards — BT24-040 cancel, EX4-060 handle — use bottom, and true top-cancel would need a new engine fn no card needs). [The "only inside kind: replacement" guard is contextual (needs clause-kind at compile time) — deferred as defense-in-depth; the cards are correct by construction.]
- [x] 3.5 Deleted the 6 superseded spec verbs (StepSpec variants + serde + field-list + 4 arg structs + 6 compile arms + the observed validator arm, replaced by a unified `place_on_security` validator). The compiled variants + engine methods stay (the unified verb lowers to them). Rewrote the 2 dsl-crate parse tests + 4 engine dsl tests onto the unified verb. Vocab regenerated (155→149 steps); drift gate green.

## 4. `link_card_to_self` → `link_cards` migration
- [x] 4.1 `link_cards` already covers single-card self-host (`to: self`) and chosen-host (`to: own_digimon`) at `count: { up_to: 1 }`. The one residual gap was BT25-060's link-gated tail ("By linking 1 …, 1 of your Digimon **may** unsuspend" = DCGO `if (linked)`): `link_cards` runs its captured tail even on a full decline (correct for AD1-005's "Then, you may delete", wrong for BT25-060) — and `link_card_to_self`'s decline-drops-tail was the only thing BT25-060 relied on. Closed by **adding a `bind_as` field to `link_cards`** (substrate widening, rule 28): it captures the linked card(s) into a `CardList` **only when ≥1 card is linked** (so `binding_present` is false after a full decline), letting BT25-060 gate the unsuspend with the universal `if { binding_present }`. `bind_as` threaded `step.rs` → `compiled.rs` (`#[serde(default)]`, bincode-safe) → `compile.rs` → `link_cards.rs` (`attach_and_continue` appends; `build.rs` touched to regen the pack). `G-DSL-LINK-N-CARDS-PER-HOST` (BT25-075, not among the 11) and `G-DSL-LINK-FROM-ANY-OWN-DIGIMON-SOURCES` (BT25-089's all-own-Digimon source scan — a pre-existing documented PARTIAL, unchanged by this migration) are out of scope here.
- [x] 4.2 Migrated all 11 cards (ST22-12, BT21-023/073/101, BT25-052/056/060/069/070/072/089) → `link_cards` (zone map `digivolution_sources → self_sources`, `chosen_own_digimon → own_digimon`, `optional: true + cost: 0 → count: { up_to: 1 } + cost: free`). BT25-060 uses `bind_as: linked_appmon` + the `if { binding_present }` gate. **121 behavioral tests across the 11 cards pass / 0 fail.** Migration is also a faithfulness improvement: `link_cards`' zone-choice-first flow matches DCGO's `SetBoolSelection` ("From hand / From sources"), where `link_card_to_self`'s union-of-zones selection did not. Only bt25_060.rs needed a test edit (`CompiledStep::LinkCardToSelf` → `LinkCards` in one structural assertion); all single-zone-per-test fixtures migrated transparently.
- [x] 4.3 Deleted `link_card_to_self`: `StepSpec`/`CompiledStep::LinkCardToSelf`, `LinkCardToSelfArgs`/`LinkFromZone`/`LinkToHost`/`default_link_*` (step.rs), the serde arms + field-list entry, the `compile.rs` lowering arm, the `lower_triggered.rs` outer-optional or-pattern arm, the whole `src/dsl_cards/step/link_card.rs` module + its `mod.rs` registration + dispatch, the 2 `link_flow.rs` TDD pins (synthetic nonzero-cost + ChangeLinkCost-reduction — no real card needs them), and the `dsl_eval_arm_coverage` include. dsl crate tests + engine lib + option_flow green; DSL JSON schema regenerates cleanly.

## 4.5 Link substrate + EX11-027 (folded in from fix-dsl-substrate-rot-and-bugs)
- [x] 4.5.1 `relink_self_to_own_digimon { host_filter?, prompt? }` (step.rs/compiled.rs/compile.rs + `link_cards.rs::try_run_relink`, dispatched in `run_step_with_runtime`) over the now-`pub(crate)` engine primitive `Game::absorb_standing_digimon_as_link` (DCGO `IPlacePermanentToLinkCards`): moves the effect's own standing permanent to a link card on a chosen OTHER own Digimon (excludes self, applies host_filter). Test `relink_self_to_own_digimon_absorbs_self_onto_filtered_other_host`. [committed `2352c734`]
- [x] 4.5.2 Added `host_filter: PredicateSpec` + `exclude_source: bool` to `link_cards` (`to: own_digimon`) — the link card's link requirement + "1 of your OTHER Digimon"; applied in `install_host_select` on top of the base Digimon/Standard gate (mirrors DCGO `CanLinkToTargetPermanent` + `permanent != PermanentOfThisCard()`). NB: now lives on `link_cards`, not the retired `link_card_to_self`. Test `link_cards_host_filter_and_exclude_source`. [committed `4e1aded7`]
- [x] 4.5.3 No new substrate — modeled with existing vocab as if-gated `select_effect_choice` (outer `count_gte` host-exists gate → inner `count_gte` hand gate → 3-way/2-way choice whose `equals` branches route to relink / link_cards / no-op). Verified via EX11-027.
- [x] 4.5.4 `cost: { place_link_card_as_bottom_digivolution: true }` → `CompiledStep::PlaceLinkCardAsBottomSourceAndCancelLeave` → `EffectContext::place_link_card_as_bottom_source_and_cancel_leave` (`Game::place_specific_link_card_as_bottom_source` = `linked_cards` → `Permanent::push_under`, no trash) + cancel_leave. Sibling of `trash_own_link_card`; same ≥1-link-card preflight. Test `place_link_card_as_bottom_source_leave_replacement`. [committed `e5ae6a35`]
- [x] 4.5.5 EX11-027 Maquinamon re-authored to pure DSL in `cards/_examples/EX11-027.yaml` (alt-digivolve `effect_text_contains`, link_condition cost 2, the on_play heterogeneous link choice, and the scope:linked place-as-bottom leave-prevention). Behavioral tests `ex11_027_pure_dsl_on_play_wires_relink_and_linkcards` + `ex11_027_linked_leave_prevention_places_link_card_as_bottom_source` (option_flow). Removed `ex11_027_*` from the phase0_exit / roundtrip `StubRegistry`.
- [x] 4.5.6 Promoted the `dsl-substrate-integrity` loader guard in `code/digimon-engine/src/cards.rs` from warn-mode to a hard error (panic on unregistered raw_rust ref) — the pack now has zero unregistered refs. Engine-level tests `unregistered_raw_rust_ref_fails_load` (synthetic pack + empty registry → rejected) + `production_registry_loads_pack_cleanly`.
- [x] 4.5.7 Moved the four `G-DSL-LINK-*` entries from `qa/dsl-vocab-gaps.md` (now a one-line resolved pointer) to `qa/resolved-gaps.md`.

## 5. Docs + verification
- [ ] 5.1 Regenerate the vocab block; confirm removed verbs drop out + new verbs appear; `dsl-vocab-doc-drift --check` green.
- [ ] 5.2 Update `RUST_DSL_AGENT_GUIDE.md` §5 (searcher pattern, `then:`-tail idiom, security placement).
- [ ] 5.3 RL guard: action-mask/tensor for a representative collapsed card matches its longhand equivalent (no action-space/tensor size change).
- [ ] 5.4 Full DSL + behavioral + action-mask suites green.
