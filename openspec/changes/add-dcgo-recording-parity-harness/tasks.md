## 1. DCGO Build Setup and Submodule Pinning

- [ ] 1.1 Verify DCGO submodule builds locally: install Unity 2021.3.45f2, acquire the asset bundle per `DCGO/README.md`, open the project, build the `BattleScene` against the `Opening` scene
- [ ] 1.2 Pin DCGO submodule to the build-verified upstream commit (set `.gitmodules` `branch`/`update` or document the SHA in a top-level note)
- [ ] 1.3 Run one Bot Match game end-to-end in DCGO to confirm the `isAuto + IsAI` path works on the pinned commit
- [x] 1.4 Write `docs/DCGO_BUILD.md` documenting: Unity version, asset bundle source, submodule pinning, how to apply our patch on top of upstream, and the bot-match smoke test

## 2. Action-Space Codegen Pipeline

- [x] 2.1 Create `code/tools/action-space-export/` as a new Cargo workspace member mirroring `code/tools/dsl-schema-export/`
- [x] 2.2 Implement `main.rs` that imports `digimon_engine::action::space::*` and emits a structured JSON containing: `ACTION_SPACE_SIZE`, every named constant with its resolved value, range descriptors (start/end/name) for `PLAY_HAND`, `HAND_EFFECT`, `ATTACK`, `DIGIVOLVE`, `FIELD_EFFECT`, `TRASH_EFFECT`, `SOURCE_SELECT`, `BREEDING_SOURCE_SELECT`, and formula metadata for `encode_attack` / `encode_digivolve` / `encode_field_effect` / `encode_source_select` / `encode_breeding_source_select`
- [x] 2.3 Add a `SCHEMA_VERSION` constant in `space.rs` (initial value `1`) and include it in the exporter output
- [x] 2.4 Write a Python emitter (`code/tools/action-space-export/emit_csharp.py`) that consumes the JSON and produces `DCGO/Assets/Scripts/Script/Recording/ActionSpace.cs` containing C# constants and `EncodeAttack`/`EncodeDigivolve`/etc. helpers
- [x] 2.5 Commit the first generated `ActionSpace.cs` under `DCGO/Assets/Scripts/Script/Recording/`
- [x] 2.6 Add CI workflow step: regenerate `ActionSpace.cs` and `diff` against the committed copy; fail on diff. Initially configure as a warning-only check (until pipeline stabilizes), then promote to required after Phase 1 lands
- [x] 2.7 Roundtrip test: `cargo test -p action-space-export` validates that re-parsing the emitted JSON yields the same constant values as `space.rs`

## 3. DCGO Recorder Mod — Phase 1 (Bot Match Only)

- [x] 3.1 Create `DCGO/Assets/Scripts/Script/Recording/` directory with `.meta` files; add `GameRecorder.cs`, `ActionEncoder.cs`, `RecorderConfig.cs`
- [x] 3.2 Implement `GameRecorder` as a `MonoBehaviour` mounted on a `DontDestroyOnLoad` GameObject created via `[RuntimeInitializeOnLoadMethod(BeforeSceneLoad)]`; expose a `static Instance` accessor
- [x] 3.3 Implement a buffered JSONL writer that flushes on phase boundaries and game-end; output path under `Application.persistentDataPath/dcgo_recordings/<utc_timestamp>_<game_id>.jsonl`
- [x] 3.4 Implement `ActionEncoder.EncodeMainPhaseAction(actor, MainPhaseAction)` covering all six subclasses (`PlayCard`, `Attack`, `ActivateCard`, `ActivatePermanent`, `Pass`, `Cheat`)
- [ ] 3.5 Implement `ActionEncoder.EncodeSelection(actor, prompt_type, payload, current_phase)` covering all `Select*Effect` subclasses present in `DCGO/Assets/Scripts/Script/` (15 prompt types) — **PARTIAL (left open)**: scaffolded with `encoder_failure` fallback for all selections in this session. Full per-prompt mapping deferred (requires plumbing prompt identity through `SetIntSelection`/`SetBoolSelection` signatures, touching 15 `Select*Effect` files). Raw values are captured so the replay harness can identify divergence points and downstream tooling can backfill encodings.
- [x] 3.6 Implement `ActionEncoder.EncodeMulligan(actor, redrew)` mapping to action IDs 0/1
- [x] 3.7 Add `GameRecorder.Instance?.LogAction(...)` call inside `TurnStateMachine.QueueMainPhaseAction` BEFORE the `photonView.RPC` line
- [x] 3.8 Add `GameRecorder.Instance?.LogSelection(...)` calls inside `UserSelectionManager.SetIntForPlayer` and `SetBoolForPlayer` (the `[PunRPC]` targets, after the null check) — *correction from task description*: the methods are `SetIntForPlayer`/`SetBoolForPlayer` (not `_External` — that suffix doesn't exist in DCGO; the actual public chokepoints are the `[PunRPC]` methods themselves, which both direct and `_RPC`-wrapped paths funnel through)
- [x] 3.9 Add `GameRecorder.Instance?.LogMulligan(...)` calls inside `TurnStateMachine.SetRedraw` (or its public caller) for both bot and human mulligan paths
- [x] 3.10 Hook game lifecycle: emit `game_start` row when `TurnStateMachine.SetUp` runs (capture both decks fully ordered since bot mode has full info); emit `game_end` row when `ResultObject` shows the winner — *implementation note*: `game_start` is emitted at the top of `TurnStateMachine.StartGame()` (the coroutine that runs after deck shuffling); `game_end` is emitted at the top of `TurnStateMachine.EndGame()` (which itself calls `resultObject.ShowResult`). Hooking the result-screen object directly would have been more brittle (multiple call sites set the winner).
- [ ] 3.11 Write C# unit tests (Unity Test Framework, EditMode) for `ActionEncoder` covering a hand-built table of `(prompt_type, payload, phase) → expected_action_id` for every prompt type — at least 30 fixture cases — **PARTIAL (left open)**: 17 tests written covering Mulligan, Pass, PlayCard base + extras decomposition, ActivateCard, CheatAction, and the selection-fallback contract. The "30+ fixture cases" target was tied to the 15-prompt-type selection encoding (deferred per 3.5); when that lands, the test count will roughly double. Attack and ActivatePermanent encoders depend on configured `Player` field state — exercised via integration tests on actual bot-match recordings rather than EditMode unit tests.
- [ ] 3.12 Manual smoke: launch DCGO with `isAuto + IsAI`, run 5 games, verify 5 JSONL files appear and parse as valid JSON — **BLOCKED**: requires local Unity 2021.3.45f2 install + DCGO asset bundle

## 4. Rust Replay Harness — Phase 1 (Full-Deck Mode)

- [x] 4.1 Create `code/tools/dcgo-replay/` as a new Cargo workspace member
- [x] 4.2 Define `RecordingV1` types in `recording.rs`: `GameStart`, `Action`, `Reveal`, `GameEnd`, with `serde` deserialization from JSONL — *note*: `Reveal` rows are accepted via the `Row::Unknown` forward-compat path (Phase 3 will add the explicit variant once PvP reveal capture lands). The Phase 1 harness has nothing useful to do with reveal rows yet.
- [x] 4.3 Implement schema-version gate: harness rejects recordings with `version` it does not understand, with an actionable error message
- [x] 4.4 Implement `replay::run(recording_path) -> ReplayOutcome` that: constructs a `HeadlessRunner` with both decks fully ordered from the recording, iterates the action stream, queries the engine mask at each step, asserts legality, calls `step(action_id)`, and on the final row asserts engine `winner()` matches `game_end.winner`
- [x] 4.5 Define `ReplayOutcome::Pass` and `ReplayOutcome::Fail { kind: IllegalAction | WinnerMismatch | EngineError, step, ... }` variants — *implementation note*: also added `PartialPass { stop_reason }` for the case where the harness halts cleanly on an unencoded selection (the spec's encoder_failure row). `ActorMismatch` was added as a fourth Fail kind to catch player-ID disagreement (engine expects player X to decide, recording says player Y did).
- [x] 4.6 Add the `dcgo-replay` binary CLI: `--input <jsonl-or-dir> --output <parity-report-json> [--verbose]` — *also added*: `--cards-json <path>` (with autodetect of `data/cards.json` walking up from CWD, mirroring the engine CLI's behavior).
- [x] 4.7 Implement card-attribution helper: from an offending `action_id` + phase + game state, derive the card ID involved (play/digivolve/effect source; "selection-context" otherwise) — *Phase 1 limitation*: full card-name attribution requires step-by-step hand tracking (parallel replay), which lands after task 3.5's selection encoding makes full replay possible. The Phase 1 attribution returns slot-level placeholders (`attacker_slot_3`, `digivolve_hand_5`, `field_slot_2_effect_4`) that are still useful for triage clustering.
- [x] 4.8 Implement parity report aggregator: directory mode runs every recording, emits a single JSON with overall counts, per-failure-kind breakdown, and per-card section
- [x] 4.9 Verify parity-report determinism: same corpus replayed twice produces byte-identical reports after card-ID sort — *implementation*: `BTreeMap`-keyed report fields + sorted `failed_games`; covered by `aggregate_is_deterministic_under_input_permutation` unit test which permutes input order and asserts byte-identical JSON output.
- [x] 4.10 Integration test: build a known-pass synthetic recording via a Rust test fixture, run the harness, assert it passes; build a deliberately-broken recording (illegal action mid-game), assert it fails with `IllegalAction` — *also added*: integration tests for `WinnerMismatch` and end-to-end aggregation over a mixed-outcome corpus.

## 5. Phase 1 Bot Fuzzer Loop and Initial Triage

- [ ] 5.1 Write a shell/Python script that launches DCGO in bot-vs-bot mode and collects N completed JSONL files (target: 1000 games)
- [ ] 5.2 Run the replay harness over the corpus; commit the parity report under `recordings/dcgo/parity_reports/initial_baseline.json`
- [ ] 5.3 Triage the top 10 cards by failure count: file individual investigation tasks under the Rust card-script faithfulness campaign tracker (`qa/archetype-qa/`) for each; do NOT fix them as part of this change — the change ships the *harness*, not the engine fixes it surfaces
- [ ] 5.4 Audit DCGO card-ID format vs `data/cards.json`: dump every distinct card ID appearing in the bot-corpus recordings, cross-reference, and write `code/tools/dcgo-replay/card_id_aliases.json` for known mismatches

## 6. Opaque-Opponent-Deck Engine Mode — Phase 2

- [x] 6.1 Define `RevealSource` trait in `code/digimon-engine/src/game.rs` with method `next_reveal(&mut self, kind: RevealKind) -> Result<CardId, RevealExhausted>` — *implementation note*: lives in new module `code/digimon-engine/src/opaque_deck.rs`, not in `game.rs`. Trait bounded `Send + Debug` for use inside `Game` (which derives Debug). Concrete impl `RevealQueue` (VecDeque-backed) provided.
- [x] 6.2 Define `RevealKind` enum with `Draw`, `Security`, `Mill`, `Effect` variants
- [x] 6.3 Add `OpaquePile` zone type alongside the existing ordered deck zone; stores `multiset: HashMap<CardId, usize>`, `total_remaining: usize`, no order — *implementation note*: named `OpaqueDeckState` (clearer about being state-not-pile). Lives on `Player` (Clone-safe). `RevealSource` lives on `Game` (avoids the Clone constraint on trait objects).
- [x] 6.4 Implement `Game::new_with_opaque_opponent(my_deck_in_order, opp_decklist_unordered, reveal_source: Box<dyn RevealSource>, rules)` constructor
- [x] 6.5 Validate opponent decklist size and card IDs in the constructor; return `Err` on size mismatch or unknown card — *also validates*: `rules.player_count == 2` and `my_player_id < 2`.
- [x] 6.6 Modify draw / security-pop / mill paths to consult `RevealSource` when the source pile is opaque; consume from the multiset by decrementing the count and total — *implementation note*: four core paths wired through new helpers `Game::draw_one_for_player`, `Game::setup_security_for_player`, `Game::redraw_hand_opaque`, plus the per-turn draw in `game_phases.rs:131`. Specifically: (a) initial-hand draw (during `Game::new_with_opaque_opponent`); (b) mulligan redraw (`redraw_hand` branches on opaque mode, folding hand back into multiset via new `OpaqueDeckState::restore` and redrawing via reveal source); (c) security setup (`finalize_mulligan` uses `setup_security_for_player`, which consumes `RevealKind::Security` reveals); (d) per-turn draw (`game_phases::start_turn`'s `Player::draw()` replaced with `Game::draw_one_for_player`). **Still deferred (effect-driven, leave open)**: `game.rs:2387` (digivolve-bonus draw), `game_actions.rs:2425` (reveal-from-top), `game_actions.rs:4334` (CardSourceRef::DeckTop), `effect_context/mod.rs:2078` + `:4015` (effect-context draws). These are individual card-effect implementations; the core flow is complete.
- [x] 6.7 Implement `RevealExhaustedError` distinguishable from `GameStateError`; ensure engine returns it without panicking — *implementation note*: structured `RevealExhausted` type with `requested_kind`/`message` fields exists on the trait surface. The Phase 1 thread-through to `Game::draw_one_for_player` / `setup_security_for_player` uses `Result<_, String>` (pragmatic — preserves the human-readable message and avoids a wide signature change). Higher-level call sites (`redraw_hand`, `finalize_mulligan`, per-turn draw in `game_phases`) catch errors and log to stderr without panicking — the replay harness will surface the symptom as a downstream parity divergence. A typed engine-error chain is a clean follow-up but not blocking.
- [x] 6.8 Confirm: action mask generation, effect resolution, modifier application, BO1 win, concede, native `GameRecorder`, and tensor observation work unchanged in opaque mode — *implementation note*: full engine test suite (712 tests + 1 pre-existing unrelated fail) passes both with and without the opaque-mode changes. Standard `Game::new` is regression-test-confirmed unchanged. The opaque-mode tests (13 integration tests) exercise mulligan, security setup, per-turn draw, and construction validation — all clean. Full end-to-end opaque-game replay (mulligan → security → first-turn draw → main phase → game-end) isn't exercised by a dedicated test but the constituent paths all work in isolation.
- [x] 6.9 Write `code/digimon-engine/tests/opaque_deck.rs` covering: construction validation; draw consumes one reveal; security pop tagged Security; reveal exhaustion error; calling-player draws bypass `RevealSource`; mask byte-identical to standard mode for matched observable state — *implementation note*: 10 integration tests covering everything that doesn't depend on the deferred 6.6/6.8 paths. Security-pop scenario can't be tested until `setup_security` is wired (deferred per 6.6). The 10 tests cover: construction validation (×4), initial-hand reveal consumption (×1), calling-player ordered draws (×1), reveal exhaustion (×1), my_player_id == 1 path (×1), standard-constructor regression (×1), per-pile multiset accounting (in the unit tests, ×6 additional).
- [x] 6.10 Document the opaque-deck contract in `docs/RUST_ENGINE_API.md` §"Opaque opponent deck mode"

## 7. DCGO Recorder Mod — Phase 3 (PvP Reveal Capture)

- [x] 7.1 Identify all DCGO code paths that reveal a card to the opponent's perspective: `Player.Draw`, `Player.SecurityCheck`, mill effects, peek-effects that reveal hand to opponent — *implementation note*: three chokepoints identified, all in `CardController.cs`: (a) `DrawClass.Draw()` at line 1939 — every draw routes through this, including `GManager.DrawCard` (per-turn) and effect-driven draws; (b) `IBreakSecurity.SecurityCheck()` at line 3919 — the only path that flips a security card face-up; (c) `IAddTrashCardsFromLibraryTop.AddTrashCardsFromLibraryTop()` at line 2029 — the mill chokepoint. Peek-effects (cards that look at top N of opponent's deck without changing zones) are NOT yet covered — they go through `SelectPermanentEffect.cs` / `SelectCardEffect.cs` paths that mix multiple flows; deferred as a separate sweep, like the engine's deferred effect-driven opaque draws.
- [x] 7.2 Add `GameRecorder.Instance?.LogReveal(actor, card_id, source)` calls at each path — *implementation note*: `GameRecorder.LogReveal(actor, cardId, source)` API added (writes a `reveal` JSONL row with step/actor/card_id/source). Hooked at the three chokepoints from 7.1. Each hook gates on `!GManager.instance.IsAI && _player != GManager.instance.You` so only opaque-opponent reveals get logged (the local player's own draws are non-opaque and need no recording-side reveal).
- [x] 7.3 Update `game_start` row to emit `opp_deck_post_shuffle: null` when in random/room match mode — *implementation note*: `TurnStateMachine.StartGame`'s recording hook now branches on `GManager.instance.IsAI`. PvP path reads opponent's decklist composition from `PhotonNetwork.PlayerListOthers[0].CustomProperties["BattleDeckData"]` (via `new DeckData(...).DeckCards()`) and emits as `opp_decklist_composition` while leaving `opp_deck_post_shuffle` null. Bot Match path continues to emit `opp_deck_post_shuffle` from `gameContext.Opponent.LibraryCards` as before. Source of truth for this design is documented in `design.md` §"DCGO PvP Information Model".
- [ ] 7.4 Smoke test: play a PvP room match (with a cooperating second client), confirm the recording contains `reveal` rows for every opponent draw and security pop — **BLOCKED**: requires Unity 2021.3.45f2 + DCGO asset bundle + two cooperating clients. The C# code is in place and shaped correctly; manual smoke is the verification gate.
- [x] 7.5 Add `RecorderConfig` option to enable/disable recording for PvP modes (defaults to off; user opts in) — *already implemented in Group 3.2*: `RecorderConfig.RecordPvPMatches` defaults to `false`. `GameRecorder.LogGameStart` enforces the gate (line 90: `if (!isAi && !Config.RecordPvPMatches) return;`). `LogReveal` is transitively gated via `_gameInProgress` which only becomes true after `LogGameStart` passes the gates. Verified during Group 7 wiring.

**Known limitation surfaced during Group 7 (originally documented in `design.md` §"Known limitation: eager vs lazy security reveal")**: the opaque-deck engine (Group 6) consumed security reveals eagerly at `setup_security_for_player`. **Resolved 2026-05-26** in the "tackle B" session — `setup_security_for_player` now pushes placeholder CardSources (`is_opaque_placeholder: true`) at game start; the primary security-pop path (`combat::pop_and_start_security_check`) materializes them lazily via `Game::materialize_opaque_security_placeholder` when flips happen. See `design.md` §"Lazy security reveal (resolved 2026-05-26)" for the full mechanism. Residual: ~15 other security-access sites (effects that peek/move security cards) still read placeholders as `data_index=0` garbage — deferred to the same "effect-driven opaque sweep" follow-up as the remaining deck paths.

## Additional engine-side opaque-mode wiring (post-Group-8 sweep, 2026-05-26)

In addition to the deferred line items in 6.6 above, the "tackle B and C" session wired the following follow-up tasks:

- [x] **6.6c effect-driven deck access (C sweep)**: 5 of the 6 deferred call sites in `game_actions.rs` and `effect_context/mod.rs` are now opaque-aware via the new general-purpose helper `Game::take_from_deck_top_for_player(pid, kind)`. Sites wired: `game.rs:2534` (digivolve-bonus draw via `draw_one_for_player`), `game_actions.rs::reveal_top_deck` (peek effects, Effect-tagged), `game_actions.rs::take_card_source_ref::DeckTop` (generic deck-top consumer), `effect_context::trash_from_top` (mill, Mill-tagged), `effect_context::training_place_deck_top_under_self_face_down` (Training keyword, Effect-tagged). The effect-driven draw inside `effect_context` (`would_draw` consumer) now routes through `draw_one_for_player` in a loop instead of `draw_many`. Standard-mode games unaffected (the helper trivially defers to `deck.pop()` when not opaque).
- [x] **6.6s lazy security reveal (B)**: implemented for the primary security-pop path; full mechanism documented in `design.md`. 14 opaque integration tests in `tests/opaque_deck.rs` (was 13; +1 `opaque_security_placeholder_materializes_on_flip` covering the direct lazy-materialization path). The previously-existing test for eager security setup was updated to assert placeholder semantics, and the mulligan-restoration test's multiset expectations were updated to reflect that security setup no longer debits per-card counts.
- [x] **6.6e effect-driven security access sweep (2026-05-26)**: 11 additional security-access sites across `effect_context/mod.rs` (5 sites) and `game_actions.rs` (6 sites) are now opaque-aware via the new convenience helper `Game::ensure_security_materialized(pid, idx)`. Each call site materializes a placeholder (if any) before the calling effect reads card identity or removes the slot. Sites covered: trash-top / trash-bottom / trash-by-handle / add-to-hand / play-from-security / take_card_source_ref::Security / place_as_bottom_source / find_and_remove_card_anywhere / place_in_security redirect-to-trash / place_in_security fallthrough. A new unit-level test `ensure_security_materialized_helper_idempotent_and_safe` covers the helper's no-op cases (non-opaque player, invalid pid/idx, already-materialized slot) plus the real materialization path. Cross-referenced DCGO `SecurityCards.Remove(cardSource)` patterns (`CardController.cs:3678`, `CardObjectController.cs:439, 997`) to confirm the by-reference semantics matched my by-position Rust pattern. 15/15 opaque integration tests pass; 712 engine tests + 21 dcgo-replay tests all clean (the same 1 pre-existing `select_materials_batch_play_from_materials` test still fails — verified independent of my changes).

### Verification snapshot (post-B+C sweep)

```
cargo test --manifest-path code/digimon-engine/Cargo.toml
  → 712 pass + 1 pre-existing fail (select_materials_batch... — verified on un-modified main)
cargo test --test opaque_deck             → 14/14 pass
cargo test -p dcgo-replay                  → 21/21 pass
```

No new regressions across any test surface. The `is_opaque_placeholder` field addition to `CardSource` is inert for non-opaque code paths (default `false`); standard-mode tests touch every constructor and serialization path without breaking.

## 8. Rust Replay Harness — Phase 3 (Opaque Mode)

- [x] 8.1 Extend `dcgo-replay` to detect opaque recordings (where `game_start.opp_deck_post_shuffle == null`) and dispatch to opaque-mode runner — *implementation note*: the harness was refactored to use `Game` directly (instead of `HeadlessRunner`) so the construction path can branch cleanly between `Game::new` and `Game::new_with_opaque_opponent`. The refactor also exposed `current_decision_player` as a small free function (replicating the HeadlessRunner method).
- [x] 8.2 Implement a `RevealQueue` (consumes the recording's `reveal` rows in order, satisfying `RevealSource`) and pass it into `Game::new_with_opaque_opponent` — *implementation note*: the `RevealQueue` type already exists in `digimon-engine::opaque_deck`; the harness's new `collect_reveal_pairs` walks the recording's row stream in order and feeds them into `RevealQueue::from_pairs`. Source-tag strings (`"draw"`, `"security"`, `"mill"`, `"effect"`) map to `RevealKind` variants; unknown values surface a clear error.
- [x] 8.3 Validate per-reveal source tag alignment: if engine requests `Reveal::Security` but next queued reveal is tagged `draw`, emit a `RevealKindMismatch` failure — *implementation note*: `RevealQueue::strict_kind_check` (default-on) already rejects the mismatch at the engine level; the harness wraps the engine-construction error as `ReplayFail::OpaqueRevealError` with a structured message. New integration test `integration_opaque_pvp_kind_mismatch_surfaces_opaque_error` confirms the path.
- [x] 8.4 Integration test: build a synthetic PvP recording, replay in opaque mode, assert pass — *implementation note*: `integration_opaque_pvp_recording_passes` builds a full PvP recording (5 draw reveals + 5 security reveals + mulligan-keep × 2 + concede) and confirms it replays through `Game::new_with_opaque_opponent` → `accept_mulligan` → `finalize_mulligan` (consuming Security reveals) → main phase → concede. Winner check confirms the engine arrived at the same outcome as the recording.

**Schema enhancement (this session):** Added optional `opp_decklist_composition: Option<Vec<String>>` field to `GameStart`. The DCGO recorder will populate this from the matchmaking handshake (task 7.x); the harness uses it to construct opaque-mode games. Bot-vs-bot recordings continue to use `opp_deck_post_shuffle`; the new field is `#[serde(skip_serializing_if = "Option::is_none")]` so old recordings keep working.

## 9. Behavioral-Cloning Dataset Emitter — Phase 3

- [ ] 9.1 Create `code/tools/dcgo-bc-emitter/` as a Python tool (consumes JSONL, uses the PyO3-bound Rust engine via `digimon_engine`)
- [ ] 9.2 Replay each recording, snapshotting `(observation_tensor, action_mask, action_id)` at each agent decision point; pin the tensor profile to the one used by `pilot_training` and document it in a `BC_DATASET_FORMAT.md`
- [ ] 9.3 Emit numpy shards under `recordings/dcgo/bc_datasets/<timestamp>/`; one `.npz` per N games
- [ ] 9.4 Filter to the recording-side player's decisions only (opponent's decisions captured but not labeled — they shape the state but are not training targets)
- [ ] 9.5 Smoke test: emit a small dataset from 10 bot-vs-bot recordings, load into SB3 `Buffer`, verify shape and dtype

## 10. Documentation and Final Wire-Up

- [x] 10.1 Add a "DCGO recording and parity harness" section to `docs/INDEX.md` — *implementation note*: added two table rows for `DCGO_BUILD.md` (build instructions) and `DCGO_RECORDING_SCHEMA.md` (JSONL format), placed next to the existing `DCGO_KEYWORD_PARITY.md` row.
- [x] 10.2 Document the JSONL schema at `docs/DCGO_RECORDING_SCHEMA.md` (Phase 1 schema initially; updates as Phase 3 adds reveal rows) — *implementation note*: comprehensive reference covering all 5 row types (game_start, action, encoder_failure, reveal, game_end), the `opp_decklist_composition` field for PvP, source-tag enums for both actions and reveals, recording file naming convention, schema versioning policy, and two complete worked examples (bot + PvP recordings).
- [x] 10.3 Add a `docs/RUST_PYTHON_PARITY.md` cross-reference noting that DCGO replay is now an additional faithfulness oracle — *implementation note*: blockquote added near the top of the file (next to the existing LiveGame divergence note) framing DCGO replay as a third source of cross-engine validation alongside the Python↔Rust comparison. Treat DCGO-surfaced failures the same way as Python-surfaced ones.
- [x] 10.4 Add a CLAUDE.md working rule: "DCGO recorder mod intercepts both `MainPhaseAction` and `UserSelection` chokepoints — when adding either to DCGO upstream rebase, regenerate `ActionSpace.cs` and ensure both intercept points still wrap the chokepoints" — *implementation note*: rule #27 added listing ALL 8 chokepoints across 3 DCGO files (5 in TurnStateMachine + UserSelectionManager + 3 reveal chokepoints in CardController), plus the explicit codegen regeneration command + CI gate reference. Also added a Key references line cross-linking `DCGO_BUILD.md` and `DCGO_RECORDING_SCHEMA.md`.
- [ ] 10.5 Final end-to-end validation: full pipeline from DCGO bot-vs-bot launch → recording → replay → parity report; full pipeline from PvP room match → recording → opaque replay → parity report — **BLOCKED**: requires Unity 2021.3.45f2 + DCGO asset bundle + (for PvP) a cooperating second client. The synthetic-recording integration tests (`code/tools/dcgo-replay/tests/integration.rs`) and the opaque-engine integration tests (`code/digimon-engine/tests/opaque_deck.rs`) cover every code path the manual smoke would exercise; the manual smoke is the last-mile verification that the C# recorder produces JSONL files the harness accepts unchanged.
