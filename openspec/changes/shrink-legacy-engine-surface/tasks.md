> Each top section is independently shippable. Order is by ascending effort/risk, not a hard dependency chain — 1–5 can proceed in parallel. Where a Rust path replaces a Python one, gate cutover on differential parity against the (scoped, bit-rot-free) Python path or the documented contract.

## 1. State redaction relocation (cheapest; clears a rule-22 violation)

- [x] 1.1 Move `code/engine_py_legacy/engine/state_filter.py` verbatim to `code/server/state_filter.py` (no logic change). **— done; created `code/server/state_filter.py` (verbatim filter + a relocation note in the docstring) and deleted the legacy copy.**
- [x] 1.2 Repoint importers: `ws_manager.py`, `ws_games.py` (and the inline import in `ws_games.py`) to the new module. **— done; `ws_manager.py:13`, `ws_games.py:14`, and the inline `ws_games.py:110` now import from `server.state_filter`.**
- [x] 1.3 Repoint `code/tests/api/test_state_filter_modifiers.py:7` to the new module (literal-dict test body unchanged). **— done; 2 existing tests still green.**
- [x] 1.4 Add an integration assertion that `filter_state_for_player(RustHeadlessGame.to_ui_json(), 1)` redacts opponent `handIds`/`handCards`/`securityIds` and leaves public zones intact. **— done; `test_redaction_over_rust_to_ui_json` builds a real `RustHeadlessGame`, asserts opponent hand/security redacted, counts + public zones (battleArea, memoryGauge) preserved, and the viewer keeps their own hand. PASSES.**
- [x] 1.5 Fix the stale `server/state_filter.py` references in `code/server/README.md` and `code/frontend/README.md` (now correct after the move). **— satisfied by the relocation: both READMEs already pointed at `server/state_filter.py` / `../server/state_filter.py`, which now resolves to the real file. No edit needed.**

## 2. Deck rules → Rust binding

- [x] 2.1 Repoint `simulations.py` (`parse_deck`, with its `split('\n')` fallback semantics preserved) onto `from digimon_engine import parse_deck`. **— done; the `except ValueError` fallback is preserved (Rust `parse_deck` maps errors to `PyValueError`).**
- [x] 2.2 Repoint `db/routers/training.py` (`summarize_deck`) onto the Rust binding. **— done.**
- [x] 2.3 Repoint `db/routers/decks.py`: route `no_restriction` through `validate_deck_for_game_mode(ids, "no_restriction")` (D3); keep `titan`/`edh_commander` as a thin Python wrapper pointed at the Rust `CardDatabase`/`CardKind` (D4); drop the dead `RESTRICTED_LIST` import. **— done; `validate_deck`/`validate_deck_for_game_mode`/`out_of_set_cards` now all from `digimon_engine`; `RESTRICTED_LIST` + `CardRestriction` imports removed. Verified: standard rejects 50× copies, no_restriction runs, the titan "Main deck must be exactly" size-error filter still matches Rust output.**
- [~] 2.4 Re-home `PlayerType` into a server-side enum module; repoint `lobby.py:25` and its `parse_deck` import. **— `parse_deck` repointed to `digimon_engine`. PlayerType re-home DEFERRED to `excise` (DESIGN ISSUE found): `InteractiveGame` compares `player_type == PlayerType.Human` using the legacy enum (`interactive_game.py:124`); a server-side enum would be a different class → identity check fails → humans treated as agents. PlayerType is consumed only by the legacy `InteractiveGame` (PvP, out of scope) and must migrate with it. Documented inline in `lobby.py`.**
- [x] 2.5 Remove the unused `InteractiveGame` re-export in `matchmaking.py:41`. **— done (it was `# noqa: F401` dead). NOTE: matchmaking is NOT fully legacy-free — it imports from `lobby`, which keeps `InteractiveGame` (PvP). So matchmaking stays transitively PvP-coupled until the excise cutover; excluded from the legacy-free guardrail.**
- [x] 2.6 Delete `deck_loader.py`'s restricted/EDEN lists once `decks.py` is migrated, OR add a cross-engine sync assertion across the remaining Rust copies (D8). **— resolved by deferral: the Python `deck_loader` lists are now production-unused (no server caller) and die with the directory at the excise deletion gate; deleting from the sunset dir now would only break excluded legacy tests. Rust-internal drift across the 3 Rust copies is already guarded by `tests/deck_tools/main.rs`. No new drift surface introduced.**
- [x] 2.7 Smoke-test a `titan` and an `edh_commander` deck save end-to-end (egg-vs-main classification via the Rust binding). **— done as a focused unit test of `_validate_for_mode` (`code/tests/api/test_decks_game_modes.py`): no_restriction/standard/edh-singleton/titan branches run without raising; edh singleton violation asserted. PASSES (full DB end-to-end needs auth+DB; the validation path is the migrated surface).**

## 3. Replay + recordings → Rust replay core

- [x] 3.1 Add a `RustReplayRunner` PyO3 surface over `ReplaySession`/`NativeAdapter` (new / seek / get_state / total_steps / current_step / is_complete) in `code/digimon-engine-py/src/lib.rs`. **— done; `RustReplayRunner` over the simpler `ReplayRunner` core, taking the recording JSON string (the DB `recording_json` value). Methods: `new(recording_json, verify)`, `step()`, `seek()`, `get_state()`, getters `total_steps`/`current_step`/`is_complete`/`is_game_over`/`winner_id`. Wheel rebuilt + installed.**
- [x] 3.2 Populate per-step `state` from `to_ui_json` (Rust `ReplayStepResult` has no `state` field; server schema requires one). **— done; `step()` and `get_state()` both emit `serialization::to_ui_json`. `step()` also carries `divergences` (verify mode) → server maps to `verification_ok`/`verification_errors`.**
- [x] 3.3 Reconcile 0-based Rust seek vs 1-based server/legacy step indexing in the wrapper. **— done; the binding is 0-based (documented); `replays.py`/`recordings.py` use it 0-based consistently (recordings' `step` is already `0..total_steps`). The replay routes were dead, so there is no live 1-based client to break.**
- [x] 3.4 Repoint `replays.py`, `recordings.py`, `state.py` off `engine_py_legacy` (`ReplayRunner`/`HeadlessGame`); fix the save gate `isinstance` to `RustHeadlessGame`; enable `record_actions` at creation (currently hardcoded `False` at `games.py:185`). **— done; all three repointed to `RustReplayRunner`/`RustHeadlessGame`; `seek` returns None so the routes seek-then-`get_state`; save gate is `isinstance(runner, RustHeadlessGame)`; `games.py:185` now `record_actions=True` (with a comment). `replays.py` maps the step dict + adds a `_seek_response` helper.**
- [x] 3.5 Recording-format compatibility gate: replay a corpus of existing `game_recordings.recording_json` rows and assert each reaches the same terminal outcome (confirm a corpus exists first; the routers may have been dead since the Rust cutover). **— done as a round-trip gate: a real `RustHeadlessGame(record_actions=True)` game (144 actions) was replayed through `RustReplayRunner` (142 replayable steps), reaching the SAME winner, with `seek(0)` working — proving the Rust `NativeAdapter` reads the recording format `RustHeadlessGame` emits. (A persisted-row corpus couldn't be confirmed in this env — the routes were dead since the Rust cutover; the format invariance makes data-compat near-zero risk.)**

## 4. Retire script_promotion

- [x] 4.1 Remove the `script_promotion` import and its two admin endpoints from `db/routers/admin_ai.py`. **— done; legacy import removed; `promote_task_card` + `promote_script` now return `410 Gone`; `GET /admin/promotions` (historical audits) kept; unused `PromotionRequest`/`TaskPromotionRequest` imports dropped. Imports clean with legacy blocked.**
- [x] 4.2 Repoint or remove `code/tests/ai_pipeline/test_ai_pipeline.py` (script_promotion test) per the broader-admin-AI decision. **— REMOVED per user decision (admin AI pipeline also retiring): dropped the `script_promotion` import + `test_promote_generated_script_updates_manifest`; replaced `test_promote_completed_review_task_card` with `test_promote_endpoints_are_retired` (asserts both endpoints 410). The retrieval/RBAC/task/batch/set-run tests (covering still-present code) are kept. File collects: 38 tests.**
- [x] 4.3 Confirm the rest of the admin AI pipeline (review / QA / issue surfaces) is untouched. **— confirmed; only the two promotion endpoints + their import changed.**

## 5. Tools de-legacied

- [x] 5.1 TIER-1 delete (no caller): `promote_script.py`, `archive/bootstrap_frozen_manifest.py`, `check_frozen_integrity.py`, `run_qa_batch.py`. Scrub references in `docs/TOOLS.md`, `AGENTS.md`, `README.md`. **— done; 4 files deleted (no importers — grep-verified). Docs: `docs/TOOLS.md` §2 replaced with a retirement note; `docs/ARCHITECTURE.md` line removed. Archived plan docs left as historical record.**
- [x] 5.2 TIER-2 re-home: inline `RE_CARD_ID` in `meta_loader.py`; delete the dead `except ImportError` legacy fallback in `resolve_deck.py`. **— done; `meta_loader` defines `RE_CARD_ID` locally (already imports `re`); `resolve_deck` now imports `CardDatabase`/`CardKind` directly from `digimon_engine` (dead fallback removed).**
- [x] 5.3 TIER-3 port: reproduce `parse_xros_req`/`parse_digixros_req` for `ingest_cards.py` (byte-identical `dna_costs`/`digixros_costs`); gate on a diff of regenerated `cards.json` against the current file. **— DONE; created standalone pure-Python `code/tools/xros_cost_parser.py` (the two parsers + helpers + `CardColor` enum + the 4 `evo_cost` dataclasses, copied verbatim). `ingest_cards._parse_xros_costs` now imports from it (legacy-free, confirmed by grep). Byte-identical GATE: ran new vs legacy parser over all 745 distinct `cards.json` `xros_req` strings through ingest's own `_dna_cost_to_json`/`_digixros_cost_to_json` serializers → 0 mismatches (51 dna_costs + 53 digixros_costs). Durable test `code/tests/api/test_xros_cost_parser.py` (smoke + legacy-parity gate, importorskip). 5 tests PASS. Avoided a full `cards.json` regen — the per-string serializer diff is the equivalent, lighter gate.**
- [x] 5.4 TIER-3 delete `run_scenario.py` after repointing the `gameplay-qa` skill at the Rust `digimon-engine-cli` scenario runner. **— RESOLVED by relocation, not delete (the premise was false): the Rust `digimon-engine-cli scenario` subcommand is a deliberate v1 STUB (`main.rs:163-167`), so there is no Rust YAML-CLI to repoint to, and `run_scenario.py` IS the legacy `ScenarioRunner` CLI (can't be de-legacied). Investigation also found the legacy YAML lane is vestigial: only 5 scenarios (all in the excluded legacy tree), the `scenario` marker is absent from `code/tests`, and the scenarios are bit-rotted (stale `deck_library.json` path). So: `git mv code/tools/run_scenario.py → code/engine_py_legacy/run_scenario.py` (co-located with the engine it serves; dies at the excise gate) → `code/tools/` is now FULLY legacy-free (grep-verified). Repointed the `gameplay-qa` skill's CLI + pytest references and flagged the lane SUNSET, directing new scenarios to the Rust `qa/scenarios/*.json` + `digimon-scenario-mcp` system. The relocated CLI imports + runs (the scenario's own data-path bit-rot is pre-existing legacy-engine breakage, not touched). Full gameplay-qa→Rust-scenario migration (incl. building the CLI scenario runner) is a separate change.**
- [x] 5.5 ~~delete `train_card_autoencoder.py`~~ → **KEEP + repoint** (user decision: keep it, point it at a new path). **— done; relocated `card_features.py` → `code/tools/card_features.py` (stdlib+numpy only; cards path now via `data_paths.CARDS_JSON`); `train_card_autoencoder` imports from `tools.card_features` and writes to `<repo>/models/`; `pilot_training`'s warm-start loader repointed to `models/card_embeddings.npy`. Tool imports legacy-free.**
- [x] 5.6 Leave `build_tested_cards.py` untouched (not a legacy importer). **— confirmed untouched.**
- [x] 5.7 Coordinate timing with `make-training-build-legacy-free` (it already stops copying these tools into `Dockerfile.training`). **— no conflict; that change already excludes these tools from the image, and the deletions are consistent with it.**

## 6. Guardrail + verification

- [x] 6.1 Extend the legacy-free import guardrail to assert (with `engine_py_legacy` blocked) clean import of the non-PvP server support routers (`simulations`, `replays`, `recordings`, `state`, `db/routers/training`, `db/routers/decks`, `db/routers/admin_ai`) and the retained tools. **— done; `code/tests/api/test_legacy_free_support_surfaces.py` (fresh-subprocess probe + negative control), two tests (support surfaces + tools; the torch-pulling autoencoder tool guarded separately). PASSES.**
- [~] 6.2 `python -m pytest code/tests -m "not slow" -q` green (no regressions). **— targeted regression GREEN: the 3 new/changed api test files + the rl legacy-free guardrail + `test_rust_bindings_surface` = 65 passed. Full `code/tests/api` dir: 175 passed / 1 failed — the 1 failure (`test_db.py::test_create_and_get_deck`) is PRE-EXISTING and unrelated (its deck includes `BT14-003`, which isn't in the `tested_cards.json` allowlist → 422 from the unchanged `_reject_untested_cards` gate, before validation). Full `-m "not slow"` suite not run end-to-end here.**
- [x] 6.3 Confirm the only remaining `engine_py_legacy` importers in `code/server/` are the PvP runtime (`ws_games.py`, `ws_manager.py`, `lobby.py` via `InteractiveGame`) — and record that this is the residual scope of `excise-legacy-engine-from-hosted-api`. **— confirmed: after this change the only `code/server/` legacy importers are `ws_games.py`, `ws_manager.py`, `lobby.py` (all via `InteractiveGame`/`PlayerType`). Recorded in the excise design findings-update (#7).**

## 7. Docs + parity

- [x] 7.1 Update `docs/RUST_PYTHON_PARITY.md` consumer table (deck-loader/state_filter/script_promotion consumers removed; tools de-legacied). **— done; dated banner note above the consumer table summarizing the deltas (legacy-free support surfaces + tools; residual PvP/`ingest_cards` coupling); `HeadlessGame` row updated. `docs/TOOLS.md` + `docs/ARCHITECTURE.md` also scrubbed.**
- [x] 7.2 Note in `excise-legacy-engine-from-hosted-api` that phases 1, 2, the module-relocation half of 3, and 5 are delivered here; its remaining scope is the PvP wire (see that change's design findings update, 2026-06-14). **— done; captured in the excise `design.md` findings-update #7 (added 2026-06-14).**
