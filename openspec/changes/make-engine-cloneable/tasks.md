# Tasks

Incremental, parity-guarded. The data-VM and legacy closure executor coexist behind a per-effect switch until the pool is fully migrated; every batch stays green against `cards_behavioral` + archetype interaction tests + the DCGO recording parity harness before proceeding.

## 0. Prerequisites & de-risking spike

- [x] 0.1 **Sequence after the in-flight DSL consolidations — LANDED (archived 2026-06-20).** `collapse-dsl-step-idioms`, `unify-dsl-scalar-and-comparators`, and `fix-dsl-substrate-rot-and-bugs` have all merged to main, so the `dsl_cards/step/` surface is now stable to start on (no concurrent-rewrite / rule-31 contamination risk). **Caveat (verified post-merge 2026-06-18):** the two Tier-0-relevant simplifications those proposals described did **not** materialize — the dp/play-cost budget verbs were NOT merged (still **7** trampolines), and active `raw_rust` is **4**, not 0 (capped at <3% of the pool by `raw_rust_budget_status`). So Tier-0 scope is essentially unchanged from the inventory; the win is a stable, conflict-free surface, not a smaller one.
- [x] 0.2 **De-risking spike — DONE 2026-06-18, expanded to the full Batch-0 substrate** (all 9 RunTail kinds + the `count_capped` MultiPickStep executor; 14 resume unit tests; see `migration-playbook.md`). Originally scoped to one trampoline: defunctionalize ONE recursive trampoline (`count_capped` — exercises the multi-pick accumulator, composition, and the decline path) behind the coexistence switch (`PendingSelection.callback` stays; add `resume: Option<ResumeStack>`; `resolve_generic_selection` runs `resume` if `Some`, else the closure). **Success criterion (corrected 2026-06-18):** `Game` cannot be cloned until the whole pool is on data frames (task 4.1), so the spike does NOT clone a live Game. Instead prove: (a) **behavioral differential** — count_capped cards routed through the data-frame path pass their `cards_behavioral` tests identically to the closure path; and (b) **round-trip** — the parked `ResumeStack` for count_capped is `Clone` + serializable in isolation. The whole-Game clone-then-replay-equals-original assertion lives in task 4.2, after the pool is migrated. Validates the frame-stack design before committing to the full-pool migration. **Progress 2026-06-18:** coexistence switch wired (`Game::pending_selection_resume` + the `resolve_generic_selection` branch dispatching to `crate::resume::run_resume`); the `RunTail` shape (~95% of selection sites) is implemented and PROVEN — `resume::tests::runtail_hand_runs_inner_tail_as_data` resolves a Hand selection whose continuation is plain data (the legacy closure is rigged to panic, so only the data path can make the test pass) and `resume_stack_is_clone` confirms criterion (b). REMAINING for 0.2: the `MultiPickStep` arm (the count_capped recursion) + porting a real card's *installer* to EMIT frames (the test constructs the frame directly for now).

## 1. Foundations (no behavior change)

- [x] 1.1 `Arc`-share the immutable registries on the clone path. *(Verified 2026-06-18: `effect_registry`, `formula_extensions`, `token_registry`, `rules`, `alt_path_registry` ALREADY derive `Clone` — effects are internally `Arc`-shared — so they do NOT block `Game: Clone`. `card_data` is `Vec<CardData>` (`Clone`); wrapping it in `Arc` for cheap clone is the D5 optimization, deferred to 4.3. `logger` is NOT `Arc`-shareable — its trait methods take `&mut self` — so it is handled by the manual `impl Clone` instead (see 4.1), not by Arc-sharing.)*
- [x] 1.2 Convert Category-A behavior closures (modifier predicates/effects) to `Arc<dyn Fn>` shareable handles. *(Done 2026-06-18: modifier predicates + granted bodies were already `Arc`; converted the last straggler `replacement::ReplacementConditionFn` from `Box<dyn Fn>` → `Arc<dyn Fn>`.)*
- [x] 1.3 `derive(Clone)`/ensure `Clone` on all plain per-game data. *(Done 2026-06-18: `Player`, zones, `EffectQueue` (`= VecDeque<QueuedEffect>`, effects stored by `card_id`+slot), counters, and `rng: StdRng` were already `Clone`; added `Clone` to the `ModifierRegistry` family (1.2) and `ParkedReplacement`. A throwaway `derive(Clone)` on `Game` confirms ALL plain per-game data is now `Clone` — the only residual blockers are the 3 non-data fields in 4.1.)*
- [x] 1.4 Add a migration tracker doc and a CI job that runs both execution paths during the transition. *(Done 2026-06-27: the migration tracker doc is `migration-playbook.md` (+ the §3.1 batch log in this file and `.claude/plans/make-engine-cloneable-remaining-installers.md`). The both-path CI job is `.github/workflows/engine-clone-safety.yml` — it runs the engine lib clone-guard unit tests (`resume::tests::game_clone_is_independent_and_replays_identically` + the per-installer `*_clones_faithfully_*` analogues) plus `cards_behavioral` (exercises resume-flipped installers through the data VM AND the not-yet-flipped ones through the legacy closure executor in the same process — the D6 parity gate) + `dsl` + `archetypes`, all with `RUST_MIN_STACK=268435456`.)*

## 2. Resumable effect VM

- [x] 2.1 Design the interpreter state: typed frame stack. *(Done 2026-06-18: `code/digimon-engine/src/resume.rs` defines `ResumeStack` + `ResumeFrame` {`RunTail`, `MultiPickStep`} + `ResumeProvenance` — all `Clone`, compiling against the real engine types (`CompiledStep`/`Bindings`/`StepRuntime`/`CompiledPredicate`/`TriggerContext`). Bytecode-vs-tree-walking Open Question resolved to **tree-walking**: the executor is already a tree-walking interpreter over `CompiledStep`, so we defunctionalize the continuation rather than rewrite to bytecode. Wiring this into `PendingSelection` (coexistence `resume` field) + porting `count_capped` is the 0.2 spike — next.)*
- [x] 2.2 Implement the interpreter over the compiled DSL AST (`digimon-dsl` `compiled.rs`/`step.rs`) behind a per-effect switch, alongside the legacy closure path. *(Done: `crate::resume::run_resume` is the tree-walking interpreter over `CompiledStep` via the `ResumeFrame`/`ResumeSelectKind` stack (`code/digimon-engine/src/resume.rs`); the per-effect switch is `Game::pending_selection_resume` — `resolve_generic_selection` runs `run_resume` when it is `Some`, else the legacy closure, which is rigged to `panic!` if reached while resume is set (resume.rs:631), proving the switch is exclusive. Coexistence guards (`if pending_selection_resume.is_some()`) are wired across the composition sites — `dsl_cards/step/mod.rs`, `effect_queue.rs`, `game_actions/{cost,misc,options}.rs`. The interpreter coexists with the closure path per the D6 plan; the remaining work is migrating callers onto it (2.3/3.1), not building it.)*
- [ ] 2.3 Convert the ~30 `select_*` helpers (`effect_context/selections.rs`) into data yields: halt with a `PendingSelection` record; resume pushes the choice and continues at the saved IP.
- [ ] 2.4 Represent pay-cost continuations, parked replacements, granted-effect bodies, and the effect queue as typed VM frames (replace the `Box<dyn FnOnce>` slots on `Game`).
- [ ] 2.5 Validate the no-approximations invariant: every choice still surfaces via `pending_selection`.

## 3. Card-pool migration (parity-gated)

- [~] 3.1 Migrate the remaining ~16 selection **installers** onto the resumable VM — **the playbook's 16 are DONE 2026-06-23, but the playbook UNDERCOUNTED** (the `cutover-selection-site-audit` found ~9 more DSL installers — `select_effect_choice`, `use_option_from_hand`, `choose_from_reveal`, `dna_pair`, `order_remainder`, `remainder_permutation_with_tail`, two tamer-cost helpers — plus the entire non-DSL selection surface, all still closure-based; see the CORRECTION under Batch 4). So this migrates the common DSL card-effect zone/permanent/reveal/material/union + multi-pick selection STEPS, NOT every selection (each installer ported migrates every card that uses it — NOT 470 card rewrites), per **`migration-playbook.md`** (produced 2026-06-18 by the `cloneable-installer-migration-playbook` Workflow — 16 read-only analysis agents + synthesis): **Batch 0** = shared core (8 new RunTail `ResumeSelectKind` variants + their `run_resume` decode arms + the `MultiPickStep` executor + a post-stack final-callback channel + unit tests); **Batch 1** = 3 mechanical RunTail flips (trash / security / breeding); **Batch 2** = 4 moderate RunTail flips; **Batch 3** = `union_zone` (dual-tail, tri-range); **Batch 4** = the 7 `MultiPickStep` trampolines (permutation → count_capped → dp-budget → play-cost-budget → reveal-bucket → source-multi → partition). Gate each batch on `cards_behavioral` + DCGO parity (`RUST_MIN_STACK=268435456`; isolated `CARGO_TARGET_DIR`).
  - **Batch 0 DONE** (commits through 7e91e1c5), **Batch 1 DONE** (commit 6aaccd265), **Batch 2 DONE** (commit 960d7dac2) — each full-pool green (`cards_behavioral` 5825/5825, `dsl` 781/781, `lib` 250/250). The flips themselves are mechanical; the value is that each batch surfaced + solved a shared substrate gap the playbook mis-rated as per-installer:
    - **Batch 1 → nested-resume outer-tail composition.** A flipped select nested as an interrupt inside another clause: the outer clause's wrapped tail must compose as DATA (`resume::OuterContinuation` on the frame, run via `drain_or_rewrap_pending_tail`), not onto the bypassed closure (proven by EX11-044). Batch 4 must extend `outer_conts` to `MultiPickStep` frames (they panic loudly on a wrap today).
    - **Batch 2 → resume-side continuation channel.** ~5 callback-continuation wrappers (play-cost / interactive-reducer / DigiXros-leave / `run_after_selections_drain`) compose a continuation onto the bypassed closure; each now defers onto `Game.after_selection_resume_hooks` (`resume::ResumeContinuationHooks`) when the select is resume-driven, drained after `run_resume` (proven by bt12_112's DigiXros play-cost continuation). Any NEW callback-continuation wrapper must add the same resume-aware guard.
    - With both substrates in place, Batch 3 (`union_zone`) and the remaining flips are genuinely mechanical.
  - **Batch 3 DONE** (commit 441df1ed1): `union_zone` flipped (last RunTail; dual-tail + tri-range). No new substrate; rode on both Batch-1/2 substrates, full pool green. **All single-pick selection kinds are now on the resumable VM.**
  - **Batch 4 COMPLETE for the production pool** (the multi-pick trampolines), all full-pool-green: **permutation** (9002566d), **dp-budget + play-cost-budget** (02a406b3, unified `BudgetStep`), **source-multi** (a0e66423), **count_capped** all 3 sub-paths (b7903656 card-based Hand/Trash + Material-sources, da7237012 BattleArea-permanents), **reveal-bucket** (5288511d, first fully-data-driven selection). Plus the **`outer_conts`-on-multi-pick extension** + the **clobber fix** (park only when our own candidates are non-empty). **partition is the one selection NOT flipped — correctly:** it's test-only (sole caller is `tests/replacements/partition.rs`; no production card/DSL verb/keyword), its requirements are raw `Box<dyn Fn>` closures (not `CompiledPredicate`s), and it never parks in a production game so it does not block `Game: Clone`. Data-port it if/when a DSL `<Partition>` card exists. See `migration-playbook.md` §"Batch 4".
  - **CORRECTION (2026-06-23, via the `cutover-selection-site-audit` workflow):** an earlier "100% of the production selection surface" claim here was **OVERSTATED**. What is on the VM is the DSL card-effect **selection-STEP** surface (the playbook's hand/trash/security/breeding/permanent/reveal/material/union + multi-pick installers). **Status as of 2026-06-28: the ENTIRE DSL card-effect selection surface is on the resumable VM** (a) — every `selections.rs` installer (`select_effect_choice`, `use_option_from_hand`, `choose_from_reveal`, `dna_pair`, single- AND multi-destination `order_remainder`, `remainder_permutation_with_tail`, both tamer-trash-cost helpers incl. multi-pick `trash_n`), the `combat.rs` (redirect + may_attack/force-attack) and `zone_moves.rs` (MayAddTopSecurityToHand) step files, AND `link_cards.rs` (both `try_run_relink` and the recursive LOOP) were flipped 2026-06-23/24/27/28, each full-pool-gated. A clone paused at ANY DSL selection kind is now faithful. **STILL closure-based (clone-UNSAFE):** (b) the **non-DSL** surface only (the remaining 6.1 effort) — the `EffectContext::select_*` primitive API, combat/keyword interrupts (`keyword_effects.rs` + `combat/mod.rs`), digivolve cost-choice + reducers, BO3 play-order (`game/lifecycle.rs`), Overclock, TriggerOrder/optional-trigger/refired-EffectChoice (`effect_queue.rs`), replacement-accept, Delay, `effect_context/action/*`. A clone at one of those panics on resolve. So the cutover (6.1) is now blocked **only** by the ~12 closure-based non-DSL subsystems (`link_cards` is no longer among the blockers; `partition` remains test-only).
  - **Post-audit progress (2026-06-23/24) — 7 DSL installers + 3 step-file sites flipped, each full-pool-green (cards_behavioral suite, 0 failed):**
    - `select_effect_choice` (commit 544d56151, new `ResumeSelectKind::EffectChoice`; the pervasive "choose one" prompt — pure index decode).
    - `choose_from_reveal` (commit 08cff8d9d, extended `ResumeSelectKind::Reveal` with an optional `RevealRoute{destination,target_player,target_permanent}` — bind then route to destination).
    - `remainder_permutation_with_tail` + single-destination `order_remainder` (commit 08cff8d9d, added optional `RemainderPlacement{player,position}` to `PermutationState` — terminal places the ordered list on the deck instead of binding).
    - `install_trash_bottom_face_down_source_under_tamer` (commit a6485825c, extended `ResumeSelectKind::FieldPermanent` with optional `post: FieldPermanentPostAction::TrashBottomFaceDownSource` — trash the picked Tamer's bottom face-down source then cost-gated tail; nested-park threads via `park_pending_selection_tail`, no bespoke channel; decline `None` per `install_field_selection`'s `on_decline: None`).
    - `install_select_dna_pair` (commit 0da707e99, new `ResumeSelectKind::DnaPairLeft` — frame-installs-frame two-pick: resolve left → bind → chain into already-flipped `install_select_any_permanent` for the right pick; `inner_tail` becomes the right tail; `outer_conts` transfer via the shared post-match `run_outer_conts`; decline `None`. + a resume.rs clone-between-picks unit test).
    - `use_option_from_hand` (commit 87b215f01, **HARDEST** — new `ResumeFrame::UseOptionFromHandStep`; the post-pick action plays an option that can install a NESTED selection, so the tail composes via `drain_or_rewrap_pending_tail` (threads onto the nested frame) not inline; mirrors the closure's accept/decline 1:1 incl. `OptionPlayResult::Invalid` early-return + continue-tail decline; added to the `wrap_pending_selection_with_tail` frame match).
    - `install_trash_n_bottom_face_down_sources_under_tamers` (2026-06-24, **the LAST pure-DSL installer** — multi-pick mutation-per-pick. **NO new ResumeFrame variant + the documented open design issue DISSOLVED** via Design 3: model the whole multi-pick as a single-pick `FieldPermanent{post:TrashBottomFaceDownSource}` frame (reuses the `install_trash_bottom_face_down_source_under_tamer` arm) whose `inner_tail` re-enters `TrashBottomFaceDownSourcesUnderTamers{of, count: remaining-1}` until `remaining==1` then the real tail; a per-pick trash that parks a nested `OnDigivolutionCardTrashed` observer threads via the SAME `park_pending_selection_tail` recursion #3 uses — so NO `after_selection_resume_hooks`, NO 3-deep re-arm. Threaded `of: CompiledPlayerRef` through the installer. Gate: bt25_035 (incl. new `bt25_035_trash_n_clones_faithfully_between_tamer_picks` clone-between-picks test), st24_06, st24_10.).
    - `zone_moves.rs:66 MayAddTopSecurityToHand` (2026-06-24, **first step-file site** — reused the existing `Security` kind, NO new frame: extended it with `post: Option<SecurityPostAction>` + `SecurityPostAction::AddTopToHand` — same `post:` pattern as the trash-bottom-under-tamer arm). The post-action `add_top_security_to_hand` drains a security-removed observer (NOT atomic), so the clause's following steps ride `outer_conts` (nested-park-safe). **Decline gotcha: the "may" decline CONTINUES the clause, so `ResumeDecline::RunTail{empty, false}` (runs outer_conts), NOT `ResumeDecline::None` (drops them).** Gate: bt24_031 (incl. new `bt24_031_security_to_hand_clones_faithfully_at_the_prompt`), bt25_030, p_137, st20_15.
    - `combat.rs:152 select_redirect_attack_target` (2026-06-24, **second step-file site — lands the new `AttackTarget` decode family**): `ResumeSelectKind::AttackTarget { attacker }` — the arm `decode_attack`s the chosen target (`SECURITY_TARGET`→player, else opponent Digimon), `validate_attack_redirect_target`, then `apply_attack_target_substitution_with_reason(EffectRedirect(prov.source_card))`. Parked in the DSL step handler (`dsl_cards/step/combat.rs` — the primitive's ONLY caller; captures `attacker` from live `pending_attack` before the primitive installs its closure). Substitution drains `OnAttackTargetChange` (NOT atomic) → empty inner_tail + outer_conts (nested-park-safe). Optional decline = `RunTail{empty,false}`. Gate: st18_14 (incl. new `st18_14_redirect_target_clones_faithfully_at_the_prompt` — clones MID-ATTACK with `pending_attack` live), ad1_012, bt25_055, ex8_050, ex10_031, bt10_042, bt19_072, bt20_036. **`combat.rs:93/102/124` (attack-open sub-machine) reuse this AttackTarget machinery next.**
    - `combat.rs:93/102/124 may_attack_now_* / force_opponent_attack` (2026-06-24, **third step-file site — the attack-OPEN sub-machine**): new `ResumeSelectKind::BeginAttack { attacker, without_suspending, ignore_summoning_sickness, optional, cost_upgrade }` — the arm decodes the target then `begin_attack_open(Effect{source,optional}, …)`, which STARTS the attack interrupt sub-machine (counter/block/alliance). **NO bespoke `after_selection_resume_hooks` channel needed (the plan over-speculated):** a parked interrupt (even a closure-based one) threads via the empty inner_tail + `outer_conts` → `run_outer_conts` → `drain_or_rewrap` (whose closure-wrap fallback composes onto closure selections too) — the SAME machinery as redirect. Parked via a shared `park_begin_attack_resume_frame` helper in the step handler (the primitives' only callers). Gate: 189 may-attack/force-attack card tests incl. new `bt16_101_may_attack_clones_faithfully_at_the_prompt` (clones at the prompt; the clone's `begin_attack_open` deletes OPP-A + self-suspends while the original stays at the prompt).
    - **MULTI-destination `order_remainder`** (2026-06-27, **frame-installs-frame, same shape as `DnaPairLeft`**): extended `ResumeSelectKind::EffectChoice` from a unit variant to `EffectChoice { post: Option<EffectChoicePostAction> }` + new `EffectChoicePostAction::OrderRemainder { positions: Vec<StackPosition>, player }` (plain data, no closures). `post: None` keeps the plain choice (bind index → tail); `post: Some(OrderRemainder)` maps the chosen label index → deck position and chains into the already-flipped `install_remainder_permutation_with_tail` (which parks its own `PermutationStep`) — `inner_tail` is NOT run in the choice arm, it becomes the permutation's tail; the post-install trigger-context restore mirrors the closure; `outer_conts` thread onto the permutation frame via the shared `run_outer_conts`. Decline `None` (a destination must be picked). Parked in the multi-dest branch of `install_order_remainder` alongside the coexistence closure. Gate: full `cards_behavioral` 5830/5830 (0 failed), `dsl` 782/782, `archetypes` 211/211, lib `resume` 17/17 — incl. new `bt25_063_order_remainder_dest_choice_clones_faithfully` (clones at the top/bottom destination prompt, drives the clone to completion, asserts independence + identical replay); production cards BT25-063 + P-167.
    - **`link_cards.rs` `try_run_relink`** (2026-06-28, `relink_self_to_own_digimon`, EX11-027 Maquinamon — the **non-recursive** link entry, one host pick → absorb; **NO new frame**): reused the existing `FieldPermanent` arm via a new `FieldPermanentPostAction::AbsorbStandingAsLink { source }` (same `post:` reuse as the security→hand flip). `select_own_permanent` installs `FieldPermanent(OwnField)` over the controller's battle area, so the host decode IS the existing arm; the arm calls `absorb_standing_digimon_as_link(source, h)` (fires `OnDigivolutionCardTrashed`/`OnLinkedCardTrashed`/`OnLink` + drains — NOT atomic → empty `inner_tail` + `outer_conts` carry the dispatcher tail / nested park). Host pick mandatory → decline `None`. Parked in `try_run_relink` alongside the closure; the dispatcher tail after `RelinkSelfToOwnDigimon` rides `outer_conts` via the step loop's wrap. Gate: full `cards_behavioral` 5830/5830, `dsl` 782/782, `option_flow` 130/130, lib `resume` 17/17 — incl. new `relink_self_to_own_digimon_clones_faithfully_at_host_prompt`.
    - **The recursive `link_cards` LOOP** (2026-06-28, **the LAST DSL-surface site — DONE**): one new `ResumeFrame::LinkPickStep(LinkPickState)` carrying `Arc<LinkCardsSpec>` + `pick_index` + a 4-variant `LinkStage` enum (`ZoneChoice` / `CardSelectHandTrash` / `CardSelectSources` / `HostSelect`) — all plain `Clone`/`Debug` data, no closures. Each stage's `install_*` helper parks the frame via `park_link_frame` alongside its legacy closure select; `run_link_pick_step` (in `link_cards.rs`) decodes the resolving action per stage and advances by **re-entering the same `install_*` helpers** (so a single source of truth for "what each stage installs") or attaches + recurses. Per the RESOLVED design the post-attach `install_pick(K+1)` runs **inline** exactly as the closure does — **no continuation channel** (the crux dissolved; clone-safety covers clones taken AT a pick prompt, not a nested OnLink-observer park, which is closure-based non-DSL). Decode reuse: `EffectChoice`/`Hand`/`Trash`/`FieldPermanent` index math + `decode_source_select` for the digivolution-source stage. Frame added to `wrap_pending_selection_with_tail`'s match; `run_outer_conts` made `pub(crate)` so the cross-module step driver can run composed tails. **Because `park_link_frame` sets `pending_selection_resume` on EVERY link selection, all 63 pre-existing link tests now route through `run_link_pick_step` — so the existing suite IS the parity gate.** Gate: full `cards_behavioral` 5830/5830 (0 failed), `dsl` 782/782, `archetypes` 211/211, `option_flow` 133/133, lib `resume` 17/17 — incl. 3 new clone-faithfulness tests covering all 4 stages (`link_loop_{host_select,zone_choice,source_select}_clones_faithfully`). **With this, the ENTIRE DSL card-effect selection surface is on the resumable VM** — a clone paused at any DSL selection kind is faithful. What remains for the 6.1 cutover is the non-DSL surface only.
  - **AUDIT-PLAN CORRECTION:** the workflow's plan called `zone_moves.rs:66` (MayAddTopSecurityToHand) + `combat.rs:152` (select_redirect_attack_target) "atomic" — VERIFIED both NON-atomic in source (each fires an observer → `drain_effect_queue` → can park). **Zero truly-atomic flips remain**; every remaining one needs the continuation channel or new state-machine machinery (the plan's "harder half"). Full recipes/order: `.claude/plans/make-engine-cloneable-remaining-installers.md`.
  - **KEY REALIZATION:** the remaining DSL installers are NOT mechanical reuses — each has a **bespoke post-selection side-effect** that must be encoded as plain data on a frame (exactly what `RevealRoute`/`RemainderPlacement`/`FieldPermanentPostAction`/`EffectChoicePostAction` did). **Remaining (flip-specs in `.claude/plans/make-engine-cloneable-remaining-installers.md`):** all `selections.rs` installers + the `combat.rs`/`zone_moves.rs` step-file sites are now flipped — `zone_moves.rs:66` security→hand DONE (2026-06-24), `combat.rs:152` redirect-target DONE (2026-06-24 — `AttackTarget` kind), `combat.rs:93/102/124` attack-open DONE (2026-06-24 — `BeginAttack` kind), multi-dest `order_remainder` DONE (2026-06-27 — `EffectChoicePostAction::OrderRemainder`). The recursive `link_cards` LOOP is **DONE 2026-06-28** (the post-attach crux dissolved — mirror the closure's inline `install_pick(K+1)`; a single `LinkPickState` frame, each stage resume-driven, no continuation channel). **The DSL selection surface is now COMPLETE.** **What remains is the non-DSL surface:** grow `EffectContext::select_*` a resume-emitting variant, port combat/keyword interrupts (`keyword_effects.rs`+`combat/mod.rs`), digivolve cost-choice/reducers, BO3 play-order, Overclock, TriggerOrder/optional-trigger, replacement-accept, Delay. Multi-session; each flip is full-suite-gated.
  - **Non-DSL surface progress (the post-DSL phase; pattern: a bespoke `ResumeFrame` variant holding the closure's captured params as plain data + an owning-module resolve method that mirrors the closure 1:1 + a `park` alongside the closure at the install site; `wrap` arm is `unreachable!` for top-level player actions never nested in a DSL clause). SEQUENCED ATTACK PLAN: `.claude/plans/make-engine-cloneable-nondsl-surface.md` (full inventory of every remaining closure install site + Wave A simple-EffectChoice / Wave B single-pick-engine-action / Wave C combat-interrupt-submachine ordering, per-site recipes, chain/atomicity/wrap-arm notes):**
    - **Digivolve cost-choice** (2026-06-28, `game_actions/digivolve.rs`, **first non-DSL flip** — rule 17, fires on every multi-cost digivolve): new `ResumeFrame::DigivolveCostChoice(DigivolveCostChoiceState{ acting_player, hand_index, field_index, source, routes })` (all `Copy` + a `Clone` `Vec<DigivolveRouteMatch>`). `Game::run_digivolve_cost_choice_step` decodes the route index → pins `pending_digivolve_route_choice` → re-enters `digivolve_from_hand_inner` (mirrors the `install_digivolve_cost_choice_prompt` callback exactly). Parked alongside the closure; `digivolve` module made `pub(crate)` so `resume.rs` can name the state. Verified a top-level player action (dispatched from `action/decode.rs`) → never nested → `wrap` arm `unreachable!`. **All 23 pre-existing `digivolve_cost_choice` + `..._archetypes` tests now route through the resume path (parity gate).** Gate: `cards_behavioral` 5830/5830, `dsl` 782/782, `archetypes` 211/211, lib `resume` 17/17, `alt_path_reachability` green — incl. new `cost_choice_clones_faithfully`.
    - **Digivolve cost-reducer chain (Wave A1+A2)** (2026-06-28, `game_actions/digivolve.rs`, `G-COST-REDUCE-ALLY-DIGIVOLVE` — the ally-suspend "reduce the digivolution cost" prompts): two new frames over ONE shared `DigivolveReducerState{ reducer_idx, reducer, acting_player, hand_index, field_index, source }` — `DigivolveReducerPrompt` (the accept/decline `EffectChoice`: PASS→zero reduction + re-enter at full cost; accept→`player_digivolve_reducer_accept`) and `DigivolveReducerSuspend` (the `OwnField` suspend pick the accept chains into: decode target→`suspend`→`consume_player_digivolve_reducer`→credit reduction→re-enter). Flipped **together** because a clone is faithful at a chain link only once every later link is also on the VM. `PlayerDigivolveCostReducer` is `Debug+Clone`; both parked alongside their closures (clone the reducer before the closure moves it). Reached only from `cost.rs:280` (a player digivolve action) → both `wrap` arms `unreachable!`. **All pre-existing reducer tests across `cards_behavioral` + `archetypes` now route through the resume path.** Gate: `cards_behavioral` 5830/5830, `dsl` 782/782, `archetypes` 211/211, lib `resume` 17/17 — incl. new `bt3_103_reducer_chain_clones_faithfully` (forks a clone at BOTH the accept prompt AND the suspend pick).
    - **Refire-effect choice (Wave A7)** (2026-06-28, `effect_queue.rs` `install_refire_effect_selection` — "activate one of this Digimon's effects again"): new `ResumeFrame::RefireEffectChoice(RefireEffectChoiceState{ effects: Vec<ReFireableEffect>, outer_conts })`. `Game::run_refire_effect_choice_step` decodes the picked effect index → `run_refired_effect`; optional PASS → no-op; then `run_outer_conts`. **Unlike the digivolve prompts this is INSTALLED BY A DSL ACTION (`refire_target_effect`), so it CAN nest mid-clause → the `wrap` arm is a REAL `outer_conts.push` (not `unreachable!`)** — the DSL-frame pattern. `ReFireableEffect` is `pub Debug+Clone`. **All refire tests (`dsl`/`effect_context`/`cards_behavioral` bt22/bt24/st9) now route through the resume path.** Gate: `cards_behavioral` 5830/5830, `dsl` 782/782, `archetypes` 211/211, lib `resume` 17/17 — incl. new `refire_effect_choice_clones_faithfully`.
    - **Option play-mode select (Wave A6)** (2026-06-28, `game_actions/options.rs` `install_option_mode_select` — dual-mode Plug-In Option "play as [Main]" vs "plug in via Link"): new `ResumeFrame::OptionModeSelect(OptionModeSelectState{ player_id, source, modes: Vec<OptionPlayMode>, cost_policy, outer_conts })`. `Game::run_option_mode_select_step` decodes the chosen mode index → `play_option_core` in that mode (defaulting Standard on a stale index) → `run_outer_conts`. **Carries `outer_conts` (DSL-frame pattern) because an effect can play an option (`use_option_from_hand`) → this nests mid-clause.** Made `options` module + `OptionSource`/`OptionCostPolicy` `pub(crate)` so `resume.rs` can name the state. **All 34 pre-existing `st22_08` (the dual-mode Plug-In) tests now route through the resume path.** Gate: `dsl` 782/782, `archetypes` 211/211, lib `resume` 17/17, all 35 `st22_08` tests (incl. new `st22_08_mode_select_clones_faithfully`); full `cards_behavioral` pending.
    - **Wave-A reassessment (2026-06-28):** the two `cost.rs` cost-reduction `EffectChoice` cousins (`cost.rs:133` play-from-hand chain, `cost.rs:346` digivolve reducer) **AND `options.rs:518`** (option-USE cost reducer, `run_interactive_option_use_reducer_pay_cost`) are **NOT simple** as the plan rated — all three are the **play-cost-reduction pipeline** (recursive chain / `wrap_pending_play_cost_continuation` / nested pay-cost parks) and depend on `CostTargetContext`/`CostReductionKey` being made `pub(crate)`+`Debug`. They belong with **task 2.4** (pay-cost continuations as VM frames), not Wave A — deferred together as a cohort. **With A6, Wave A's genuinely-simple `EffectChoice` flips are DONE.** **Next: Wave B** (Overclock, BO3 play-order, DigiXros material, DNA digivolve, replacement-accept) — single-pick engine-action frames.
- [x] 3.2 Migrate the nastiest multi-pick / pay-cost / replacement archetypes early to validate the frame-stack design (or simplest-first per the resolved Open Question). *(Done via Batch 4, §3.1 — the Open Question resolved "nastiest-first", and Batch 4 flipped all 7 multi-pick/pay-cost trampolines (permutation, dp-budget + play-cost-budget unified `BudgetStep`, source-multi, count_capped's 3 sub-paths, reveal-bucket) plus the `outer_conts`-on-multi-pick extension + the clobber fix — each full-pool-green (`cards_behavioral` 5825/5825). These are exactly the frame-stack's hardest shapes, so the design is validated. `partition` is the lone unflipped trampoline, correctly so: test-only, raw-closure requirements, never parks in a production game.)*
- [x] 3.3 Port or constrain the remaining `raw_rust` effects to be clone-safe — **DONE 2026-06-23** (commit 95aeef8a8). Audit: `bt24_012` (no-op), `lm_027` (atomic `add_pending_security_to_hand`), `bt21_093` (no-op stub) are clone-safe; `bt13_040_may_play_veemon_from_hand_or_source` installed a CLOSURE-based UnionZone `pending_selection` (the only clone-unsafe raw_rust) but was already DEAD (BT13-040.yaml uses pure-DSL `select_union_zone` + `play_union_bound_free`, now on the VM; no YAML/test referenced the fn) — deleted it + its registration. raw_rust 4→3, all clone-safe. (NOTE: this 3.3 work makes the *raw_rust* paths clone-safe; it does NOT mean clone is faithful everywhere — see the CORRECTION under 3.1: the broad non-DSL + several DSL selection sites are still closure-based.)

## 4. Make Game cloneable

- [x] 4.1 Implement `Clone` on `Game`. **DONE 2026-06-18:** `Game` now `#[derive(Debug, Clone)]`. The 3 non-data fields were made `Clone` without the full pool migration: `impl Clone for PendingSelection` (clones data; closure callbacks become a loud panic-stub — a cloned selection MUST be driven via the resume path); `clone_box` on `GameLogger`/`RevealSource` + `impl Clone for Box<dyn ...>`. So `Game: Clone` works **today** for any no-pending-selection state and any resume-path (migrated) selection; a closure-based (unmigrated) selection clones with a panic-stub until its installer is ported (rule: clone is faithful exactly where the resumable VM drives resolution). Registries were already `Clone` (effects internally `Arc`-shared) — no Arc-wrapping needed. **MILESTONE (verified 2026-06-18 via a throwaway `derive(Clone)` on `Game`):** after Phase-1 foundations, exactly THREE fields block `Game: Clone` — (1) `pending_selection: Option<PendingSelection>` (the `Box<dyn FnOnce>` callbacks — gated on the VM/spike), (2) `logger: Box<dyn GameLogger>`, (3) `reveal_source: Option<Box<dyn RevealSource>>`. Use a **manual `impl Clone`** (not `derive`): clone all data fields, and on the clone set `logger = Box::new(SilentLogger)` and `reveal_source = None` (both are external/diagnostic state a forked search node should not inherit). So once `pending_selection` is data, `Game: Clone` is a small manual impl away.
- [x] 4.2 Add the clone-independence and clone-replays-identically guard tests. *(DONE 2026-06-18: `resume::tests::game_clone_is_independent_and_replays_identically` — clones a Game at a resume-path decision point, resolves the clone, asserts the original is untouched (independence) and that resolving the original with the same input reaches identical state (replays identically). 238/238 lib unit tests green.)*
- [ ] 4.3 (Optional, profiled) introduce structural sharing / COW for zones to keep clone near-O(1) for untouched state.
- [ ] 4.4 (Optional) add `Serialize`/`Deserialize` for game state if save/load is in scope (resolve Open Question).

## 5. raw_rust clone-safety enforcement

- [x] 5.1 Guard against a `raw_rust` effect parking a non-`Clone` continuation — **covered by the runtime panic-stub** (`impl Clone for PendingSelection`, `selection.rs:299`): a closure-based selection clones to a callback that `panic!`s with a clear "drive it via the resumable VM" message when resolved, so a cloned clone-unsafe park fails loudly rather than silently mis-resolving. After 3.3 there are zero clone-unsafe raw_rust fns, so nothing trips it today. (A static source-scan lint was considered and rejected as brittle — self-match on its own needle, false positives — for marginal value over the panic-stub.)
- [x] 5.2 Amend CLAUDE.md rule 28 with the clone-safety constraint — **DONE 2026-06-23**: rule 28 now states a `raw_rust` effect MUST be atomic OR drive selections through the resumable VM (`ResumeStack`/`ResumeFrame` + `pending_selection_resume`), never a bespoke closure `pending_selection`; cites the panic-stub + the `RevealBucketStep` fully-data-driven example. (`formula_extensions` are pure read-only formula evaluators — no parked continuation — so they need no clone-safety treatment.)

## 6. Cutover & docs

- [ ] 6.1 Delete the legacy closure executor once the pool is fully migrated and both-path CI is green. **BLOCKED (2026-06-23 audit; step (1) nearly complete as of 2026-06-24):** the pool is NOT fully migrated — the closure executor (`resolve_generic_selection`'s `else { sel.callback }` + `on_decline` branches, `effect_queue.rs`) is still the live resolver for the ~12 closure-based non-DSL subsystems (all reachable in production: Alliance on common attacks, BO3 play-order every match, digivolve cost-choice, etc.). Staged path before deletion: (1) **DONE 2026-06-28 — the entire DSL selection surface is flipped** — every `selections.rs` installer (incl. `select_effect_choice`, `use_option_from_hand`, `choose_from_reveal`, `dna_pair`, single- AND multi-destination `order_remainder`, `remainder_permutation_with_tail`, both tamer-trash-cost helpers), the `combat.rs` (redirect + may_attack/force-attack) and `zone_moves.rs` step files, AND `link_cards.rs` (`try_run_relink` + the recursive LOOP) were flipped 2026-06-23/24/27/28, each full-suite-gated; (2) grow the `EffectContext::select_*` primitive API a resume-emitting variant (so `keyword_effects.rs` + `effect_context/action/*` callers become data-driven); (3) port the non-DSL subsystems (combat interrupts, digivolve cost-choice/reducers, Overclock, TriggerOrder/optional-trigger, play-order, replacement-accept, Delay) via Rule-sourced `ResumeFrame`s; (4) rewrite the ~8 callback-composition wrapper sites onto `after_selection_resume_hooks` unconditionally; (5) ONLY THEN delete the executor branch + remove the non-Optional `PendingSelection.callback` field. Keep both-path CI green throughout. `partition` (test-only) may stay closure-based until a production `<Partition>` card exists.
- [x] 6.2 Update `docs/RUST_ENGINE_API.md` — **DONE 2026-06-23, corrected by the audit**: added a "Snapshot alternative now exists" note to the reset-and-replay contract §, documenting the resumable data VM (`pending_selection` + `pending_selection_resume`, `run_resume`). The faithfulness claim was corrected from the original "every production decision point" overstatement to: faithful at the **DSL card-effect selection-step surface** (now the whole DSL surface except `link_cards.rs`), NOT the broad non-DSL surface — see the 3.1 CORRECTION. Replay-stepper conversion flagged as a separate follow-on (back-step contract unchanged).
- [x] 6.3 Note the equilibrium-methods horizon — **DONE 2026-06-23, corrected by the audit**: updated `docs/MODEL_EVALUATION.md` §6 to state the forking precondition (`Game: Clone`) is **PARTIALLY met** — faithful at the flipped DSL selection-step kinds, NOT yet at the broad non-DSL surface (combat/digivolve/play-order/etc.), so clone-based search is currently limited to games paused at those kinds. What additionally remains for Deep CFR / ReBeL / Player of Games is the infoset/belief structure + determinization seam (the `add-determinized-search` change).

## 7. Validation

- [x] 7.1 `openspec validate make-engine-cloneable --strict` passes. *(Verified 2026-06-27: "Change 'make-engine-cloneable' is valid".)*
- [ ] 7.2 Full `cargo test --manifest-path code/digimon-engine/Cargo.toml` green, including `--test cards_behavioral` and the archetype tests.
- [ ] 7.3 DCGO recording parity harness green across the migrated pool.
- [ ] 7.4 Clone guard tests green; raw_rust clone-safety guard green.
