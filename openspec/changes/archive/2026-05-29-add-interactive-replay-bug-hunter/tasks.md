## 1. Engine: cheap Game snapshot/restore

- [x] 1.1 Audit `Game`'s fields for clone cost and non-`Clone` members; produce a short note listing what must be Arc-wrapped (`card_data`, registries) and what is intentionally non-`Clone` (`Box<dyn RevealSource>`) — see `notes/game-clone-audit.md`. **Surfaced a design blocker: the mutable game graph is pervasively closure-bearing (`ModifierEntry` not-Clone, `pending_selection` callbacks, parked continuations), so a full-state snapshot is infeasible without an engine-wide serializability refactor. Recommends Arc + reset-and-replay instead.**
- [x] 1.2 Implement `Game::reset_for_replay(&mut self)` — in-place reset of all mutable/transient/accumulator state (zones, modifiers, effect queue, every `pending_*` slot, events/event_seq, counters, mulligan state, replacement state) to defaults, **reusing the existing `card_data` + registries** (no `Game::new`, no `CardData` clone); kept adjacent to the `Game` struct (game.rs, after `Game::new`) with a maintenance-invariant doc comment
- [x] 1.3 Make backward `seek` + a `restore(step_n)` primitive use `reset_for_replay` + `relay_initial_state` (no `Game::new` on backward seek) — refactored `replay.rs`: `build_game` now delegates to the shared `relay_initial_state`; removed `snapshot_card_data`. Opaque reveal-cursor re-attach is wired in Group 4 (`DcgoAdapter`)
- [x] 1.4 Guard test `reset_for_replay_restores_defaults` (game.rs) dirties a broad mutable-field spread and asserts reset-to-default + immutables preserved; `backward_seek_resets_in_place_via_reset_for_replay` (replay.rs) exercises the backward path. Both pass
- [x] 1.5 Full engine suite run: 3449 + 170 lib pass; **0 new failures from this change**. 3 failures (`ex7_030`, `p_134`, `p_197` — a "when attacking −DP" regression) verified PRE-EXISTING on baseline (changes stashed → same 3 fail). Out of scope for this change

## 2. Engine: ReplaySession + RecordingSource adapters

- [x] 2.1 Defined `RecordingSource` trait (`build_initial_game` / `relay_initial_state` / `steps`) + normalized `StepSpec { actor, action_id, phase, source, + recorded verify fields }` in `runners/replay.rs`. (`reveal_feed` folded into the `DcgoAdapter`'s own construction in Group 4 — the trait stays format-agnostic.)
- [x] 2.2 Added `ReplaySession` parameterized over `Box<dyn RecordingSource>`, owning the `Game` + cursor. (Persistent divergence-log + `StepPolicy` are Group 3; reset-and-replay replaces the "checkpoint ring" per the task-1.1 audit.) Legacy `ReplayRunner` kept as the native-only type pending a trivial collapse to `ReplaySession` once the CLI + integration tests migrate.
- [x] 2.3 `NativeAdapter` holds the canonical native relay logic (initial-state restore, Python 1/2 ID translation, mulligan filtering); parity proven by `replay_session_round_trip_matches_replay_runner`
- [x] 2.4 Reset-and-replay `seek` (backward = `Game::reset_for_replay` + `source.relay_initial_state` + replay forward; no `Game::new`, no snapshot) + `step_back` + `restore` on `ReplaySession`
- [x] 2.5 Ported scenarios in `tests/replay_runner.rs` (forward-seek == sequential, round-trip parity, unknown-card via `NativeAdapter::from_recording`) + lib unit coverage. **All pass**
- [x] 2.6 `replay_session_seek_backward_equals_fresh` + `replay_session_step_back_decrements_and_matches_fresh` (real multi-action recording). **Pass**
- [x] 2.7 `LiveGame::from_recording` / `from_recording_at_step` + `decode_labels_via_replay` wired to `ReplaySession`. Lib tests (170, incl. LiveGame) + recording_replay_regressions + CLI/MCP build all green

## 3. Engine: per-step policy + divergence

- [x] 3.1 `StepPolicy { Trust, CheckThenApply }` + `Divergence { step, action_id, actor, phase, kind }` with `DivergenceKind` taxonomy (`MaskMiss { sample_legal_ids }`, `Actor`, `Memory`, `Phase`, `Winner`, `RevealKind`, `RevealExhausted`) — serde-tagged. `Winner`/reveal variants are populated by the DCGO path (Group 4)
- [x] 3.2 `CheckThenApply` in `ReplaySession::step`: actor check (`current_decision_player`) + mask-membership (`build_action_mask`) before applying; on mismatch records a `Divergence` and pauses (`paused` flag; no apply, no abort, no cursor advance). `seek`/`run_to_completion` honor the pause; backward reset clears the log + pause
- [x] 3.3 `Trust` applies directly (existing path); `RecordingSource::default_policy()` derives the default (Native → Trust), overridable via `ReplaySession::set_policy`
- [x] 3.4 Tests in `tests/replay_runner.rs`: CheckThenApply passes a legal recording; pauses + records `MaskMiss` (with non-empty legal sample) on an injected illegal action without advancing; Trust ignores illegality. **13/13 replay_runner + 170/170 lib pass**

## 4. Engine: DCGO adapter (move parser in)

- [x] 4.1 Moved `RecordingV1` parser to `code/digimon-engine/src/dcgo_recording.rs` (registered in `lib.rs`); `tools/dcgo-replay/src/recording.rs` is now a `pub use digimon_engine::dcgo_recording::*` shim. dcgo-replay tests still pass (13); the 8 parser tests now live in the engine lib (178 total)
- [x] 4.2 `DcgoAdapter` dispatches on `opp_deck_post_shuffle`: bot → `Game::new` (both ordered decks, seed 0); opaque → `Game::new_with_opaque_opponent` + `RevealQueue::from_pairs(reveal_stream)`. Steps = `Action` rows up to the first `encoder_failure` (DCGO replays mulligan too). Default policy `CheckThenApply`. `ReplaySession::from_dcgo` convenience added
- [x] 4.3 Opaque reset re-attaches a fresh `RevealQueue` at cursor 0 (DCGO `relay_initial_state` rebuilds deterministically; documented: DCGO backward-seek rebuilds rather than restoring in-place — batch never back-seeks, so only interactive DCGO back-step pays it). `OpaqueDeckState` is recreated by the rebuild
- [x] 4.4 Tests in `tests/replay_runner.rs`: bot reconstructs deterministically + lands in Mulligan + replays under Trust; opaque reconstructs via the opaque path deterministically; bot backward-seek rebuilds to fresh state. **17/17 replay_runner + 178 lib + 13 dcgo-replay pass**
- [x] 4.5 `map_divergence` in `dcgo-replay/replay.rs` maps the divergence log's first entry to `ReplayFail` (`MaskMiss`→IllegalAction, `Actor`→ActorMismatch, `Winner`→WinnerMismatch, `RevealKind`/`RevealExhausted`→OpaqueRevealError, `Memory`/`Phase`→EngineError) — done alongside Group 6.1

## 5. Engine: partial-observability surfacing

- [x] 5.1 Added `card_id_or_placeholder` helper + `OPAQUE_PLACEHOLDER_CARD_ID` sentinel in `view/mod.rs`. `SecurityView` gains a parallel `placeholders: Option<Vec<bool>>` and renders unrevealed cards as the sentinel; `HandCardEntry` gains `is_placeholder`; `PermanentView` gains `top_is_placeholder` and renders placeholder stack sources as the sentinel — no zone exposes a placeholder's (meaningless) concrete id
- [x] 5.2 `opaque_security_placeholder_renders_hidden_then_concrete` (tests/replay_runner.rs): god view shows slot-0 as `<hidden>` with `placeholders=[true,false]`, then reads concretely after the slot is materialized. **18/18 replay_runner + 178 lib pass**

## 6. Tool: reduce dcgo-replay to a batch driver

- [x] 6.1 `replay_recording` now builds `ReplaySession::from_dcgo`, `run_to_completion()`, and maps the divergence log → `ReplayOutcome` (+ encoder_failure→PartialPass, winner check, opaque construction error→OpaqueRevealError). Deleted the now-duplicated `build_game`/`collect_reveal_pairs`/`derive_opp_decklist`/`current_decision_player`/`sample_legal_ids` (logic lives in the engine's `DcgoAdapter`) — one code path for batch + interactive
- [x] 6.2 `ReplayConfig` + CLI flags + `report::aggregate` unchanged; `aggregate_is_deterministic_under_input_permutation` passes
- [x] 6.3 `cargo test -p dcgo-replay` → 13/13 (opaque-pass, opaque-no-reveals→OpaqueRevealError, encoder-failure→PartialPass, premature→WinnerMismatch, illegal-action→IllegalAction w/ legal sample)
- [~] 6.4 No real DCGO corpus in-tree (Unity-generated recordings are blocked, same as the parity harness's manual smokes). Behavior is covered by the synthetic-recording integration tests routed through the **unchanged** `report::aggregate`; the determinism test pins byte-stability. Full before/after corpus diff deferred to when a recording corpus exists

## 7. MCP: stepping, step-view, scanners

- [x] 7.1 `load_recording` now auto-detects native JSON (object with `actions`/`initial_state`, Trust policy) vs DCGO JSONL (leading `game_start` row, CheckThenApply) via `parse_jsonl`, with an optional `source_format` override; returns `{ ok, game_id, total_steps, source_format }`. New `LiveGame::from_dcgo_recording` carries the DCGO path. Verified by `load_recording_native_reports_total_and_format` + `load_recording_autodetects_dcgo_jsonl`
- [x] 7.2 `ReplayStepView` payload (`cursor`/`total_steps`/`is_complete`/`paused`/`policy`, decoded `recorded` action via `explain_action`, `legal_now` via `legal_decoded_actions`, `divergences`, serialized `events`, `ReplayDelta`, `card_ids_in_play` with `<hidden>` for opaque tops) built by `LiveGame::replay_step_view_inner`. Verified by `step_forward_view_carries_action_legal_set_and_delta`
- [x] 7.3 Folded a `ReplayDriver` into `LiveGame` (extracted from `ReplaySession` so both share one verified stepping core); added `step_forward`/`step_back`/`replay_seek`/`replay_restore`/`replay_step_view`, all rejecting non-recording games with `LiveGameError::NotARecording`. MCP tools `step_forward`/`step_back`/`seek`/`restore_checkpoint`/`replay_step_view` wired. Verified by `step_forward_then_back_round_trips_cursor`, `seek_and_restore_checkpoint_move_the_cursor`, `stepping_a_non_recording_game_returns_ok_false`
- [x] 7.4 `scan_divergences(stop_at_first)` (forces CheckThenApply, advances past pauses on the oracle trajectory when `!stop_at_first`), `scan_fizzles` (EffectFizzled leads), `scan_panics` (silent no-op leads) — all save+restore the caller's cursor + policy. Verified by `scanners_do_not_perturb_cursor` (lib) + `scan_divergences_finds_masked_out_action` (MCP, asserts cursor restored)
- [x] 7.5 Replaced the stubbed `seek` ("not supported in v1") with the real driver; added a "Replay stepping & scanning" tools/list section; count assertions updated 26→33 in both the unit and integration suites
- [x] 7.6 MCP-layer dispatch tests added (`code/digimon-engine-mcp/src/tools.rs`): native + DCGO `load_recording`, step-forward/back round-trip, seek/restore, step-view carries action+legal+delta, `scan_divergences` finds the masked-out action (999) and restores the cursor. **16 unit + 14 integration pass**
- [x] 7.7 `digimon-engine-mcp` is already registered in `.mcp.json` (`target/debug/digimon-engine-mcp.exe --pool implemented`); the binary builds clean (confirmed in Group 2). `tools/list` verification from Claude Code is a launch-time check (requires client restart)

## 8. Skill: /replay-bug-hunt

- [x] 8.1 Authored `.claude/skills/replay-bug-hunt/SKILL.md` (frontmatter + Phase 0 loads via `load_recording` and branches on `source_format`: `dcgo`→Mode 1, `native`→Mode 2). Documents the MCP tool surface + step-view shape and resolves `$BASE_DCGO` per rule 29
- [x] 8.2 Mode 1 differential playbook: `scan_divergences(stop_at_first)` → `seek` to the divergence → `step_back` to inspect the lead-up → localize the `mask_miss`/`actor`/`winner`/`reveal_*` to a card → confirm vs `$BASE_DCGO` C# + `general_rule.pdf` (§16 keyword rule numbers). Treats a masked recorded action as a Rust-bug signal, not a recording error
- [x] 8.3 Mode 2 judge playbook: `scan_fizzles`/`scan_panics` as leads → `step_forward` reading `recorded`/`events`/`delta` + `inspect_card` → judge each effect vs card text + rules PDF + C# → per-effect `faithful`/`not-faithful`/`blocked` verdict. Explicitly excludes `<hidden>` cards and RNG-replay non-determinism. **Added a no-op-ratio fidelity gauge** (the dry-run surfaced that a high `scan_panics` ratio means the replay diverged early — localize the first divergence, distrust later steps)
- [x] 8.4 Findings routing: engine-primitive → `docs/RUST_ENGINE_GAPS.md`, card-effect → `qa/archetype-qa/engine-gaps.md`, with a finding template recording recording-path+step+game_id, divergence kind / verdict, card, and source consulted (C# location + PDF rule). Skill states it does NOT fix the engine
- [x] 8.5 Dry-ran the skill end-to-end through the **real MCP binary** (stdio JSON-RPC, threading game_ids): **Mode 2** on the native fixture `tests/recordings/g_dsl_trash_sources_stale_handle.json` (load→native, 85 steps; `scan_fizzles`=0, `scan_panics`, `replay_step_view`@0 decodes "Hatch from egg deck" + 3 legal, `step_forward`→cursor 1); **Mode 1** on a synthetic divergent DCGO JSONL (load→dcgo, 3 steps; `scan_divergences` flags `mask_miss` at step 2 / action 999 / Main; `seek` to it). The dry-run found a precision bug in `scan_panics` (flagged 64/85) → tightened the no-op test to the engine's own `advanced` semantics (event_seq + phase + pending-selection fingerprint) and added `scanners_are_silent_on_a_faithful_recording` (0 false positives on a clean recording; the fixture's residual no-ops are genuine replay-divergence drops in a panic-regression recording)

## 9. Docs

- [x] 9.1 `docs/DEBUG_MCP.md` updated: tool surface 26→33 with the new "Replay stepping & scanning (7)" section + step-view shape; `load_recording` documents native/DCGO auto-detection + `source_format`; `seek` now a real tool; added Recipe 4 (the `/replay-bug-hunt` funnel→microscope flow); replaced the "v1 limitations" block (dropped "seek stubbed" + "no branching", added reset-and-replay / RNG-divergence / one-directional-Mode-1 notes)
- [x] 9.2 `docs/RUST_ENGINE_API.md` gained "Reset-and-replay contract (recording back-step — 2026-05-29)" after the Deletion lifecycle section: documents `Game::reset_for_replay` (in-place mutable reset, immutables reused, guard test + maintenance invariant), backward-seek = reset + `relay_initial_state` + replay-forward, the opaque reveal-cursor checkpoint (fresh `RevealQueue` per reset; DCGO rebuilds, native resets in place), and the post-seek handle-invalidation caveat
- [x] 9.3 `docs/INDEX.md` DEBUG_MCP row now names the stepping/scanner tools + the `/replay-bug-hunt` microscope ↔ `dcgo-replay` funnel relationship; CLAUDE.md Documentation section gained an "Interactive replay bug-hunting" bullet (Mode 1/2, microscope/funnel, reset-and-replay pointer)
