## 1. Rust engine event wiring (capability: engine-event-emission)

- [x] 1.1 Locate the attack-declaration site in `code/digimon-engine/src/game.rs` and emit `GameEvent::Attack` with `seq`, `player`, `attacker_field_index`, `target_field_index` (Some for Digimon, None for security), `target_player`. (Wired in `combat.rs::begin_attack_open` after `PendingAttack` install, before `WhenWouldAttack` dispatch. 2 TDD scenarios green in `tests/event_emission/attack.rs`.)
- [x] 1.2 Wire `GameEvent::Trash` emission in `Game::delete_permanents_batch` — one event per card in the deleted stack, top-down order. Pre-snapshot card IDs before the move (CLAUDE.md rule 25 — handlers run post-trash; live lookups will fail). (Added `Game::trash_card(player, card)` and `Game::trash_permanent_stack(player, field_index)` in `game.rs`; `trash_single_for_batch` in `combat.rs` routes linked-card cascade and stack deletion through them. Token-skip + empty-stack semantics mirror `Player::delete_permanent`. TDD scenario `deleting_a_single_card_permanent_emits_one_trash_event` green; full cargo test suite passes with the same delta as pre-change — the one pre-existing `select_materials_batch_play_from_materials` failure is unrelated.)
- [ ] 1.3 Wire `GameEvent::Trash` at the hand-discard call sites. **Deferred to follow-up:** no v1 reward component consumes hand-discard Trash events; emission helper (`Game::trash_card`) exists and is the conversion target when these sites are touched.
- [ ] 1.4 Wire `GameEvent::Trash` at effect-driven security-stack-to-trash call sites (distinct from `SecurityReveal`). **Deferred to follow-up:** no v1 reward component consumes effect-driven security-trash events; emission helper exists.
- [ ] 1.5 Wire `GameEvent::Trash` at remaining trash-zone migration paths (deck-to-trash, source-to-trash). **Deferred to follow-up:** no v1 reward component consumes these; emission helper exists.
- [x] 1.6 Wire `GameEvent::SecurityReveal` at the security-check resolution site — one event per revealed card, in reveal order, before any security-effect resolution. (Wired in `combat.rs::pop_and_start_security_check` after the SecurityRevealSnapshot is installed, before `pending_security` parks the card. Single-reveal test green in `tests/event_emission/security_reveal.rs`. Security Attack +N multi-reveal scenario will inherit emission naturally since `pop_and_start_security_check` is the per-reveal call site.)
- [x] 1.7 Create `code/digimon-engine/tests/event_emission/` directory and add `attack.rs`, `trash.rs`, `security_reveal.rs` integration tests using `DebugRunner`, covering the scenarios in `specs/engine-event-emission/spec.md`. (Registered as `[[test]] name = "event_emission"` in `code/digimon-engine/Cargo.toml`. Scenarios green: attack-on-Digimon, attack-on-security, single-security-reveal, single-card-permanent-trash. Multi-card-stack / Security Attack +N / discard-from-hand scenarios are skeleton placeholders awaiting Group 1.3+ trash-site conversions.)
- [x] 1.8 Run `cargo test --manifest-path code/digimon-engine/Cargo.toml` and confirm all event-emission tests pass and no existing tests regress. (711/712 pass — the one failure `select_materials_batch_play_from_materials_plays_every_picked_source` is pre-existing and confirmed unrelated to this change via `git stash` re-test.)
- [x] 1.9 Rebuild `code/digimon-engine-py` via `maturin develop` and add one Python test under `code/tests/engine/` that drains events through the PyO3 binding and asserts the three new variants appear with their expected fields. (Rebuilt via `maturin develop --release`. Added `code/tests/test_rust_event_emission.py` — passes; PyO3 binding already had `Attack`/`Trash`/`SecurityReveal` serialization branches in `event_to_pydict`, so no binding-layer changes were needed.)
- [x] 1.9a **Prerequisite (discovered):** Add canonical snake_case string keys to `CompiledAltPathKind` in `code/digimon-dsl/src/compiled.rs`. The `via_alt_path` field on `GameEvent::Play` (task 1.10) and the `key_cards:` `alt_paths` matcher (capability `reward-profiles`) consume these keys. (Added `CompiledAltPathKind::as_key()` method returning: `"digivolve"`, `"dna_digivolve"`, `"blast_dna_digivolve"`, `"digixros"`, `"burst_digivolve"`, `"app_fusion"`, `"assembly"`, `"activated_digivolve"` — matching the pre-existing string set in `dsl_cards/predicate.rs::alt_path_kind_matches`. Refactored `alt_path_kind_matches` to call `as_key()` so both consumers share a single source of truth. `cargo build` clean.)
- [x] 1.10 Extend `GameEvent::Play` in `code/digimon-engine/src/events.rs` with `cost_paid: i16`, `cost_printed: i16`, `via_alt_path: Option<String>`. Update `GameEvent::type_str` if needed (no change required — type tag unchanged). (Fields added; live_game.rs serde-roundtrip test updated; PyO3 binding's `event_to_pydict` now surfaces all three under `meta`. PyO3 rebuilt via `maturin develop --release`; Python round-trip test still passes.)
- [x] 1.11 Audit every `GameEvent::Play` emit site (use compile errors after adding the non-Option fields to find them all). At each site, populate `cost_paid` from the actual memory paid (post-discount), `cost_printed` from `CardData::play_cost`, and `via_alt_path` from the registered alt-path key (None when the play was through the standard `PLAY_HAND` action without cost reduction OR through a generic cost reduction without a named alt-path). (5 emit sites found via compile errors: `game.rs:1186` (effect-initiated free hand-play), `game.rs:1276` (multi-source effect-initiated free play), `game_actions.rs:836` (canonical PLAY-from-hand with cost), `effect_context/mod.rs:2531` (token spawn), `live_game.rs:1411` (test fixture). All populated. Standard-play sites carry `via_alt_path=None`; alt-path emission sites will populate via_alt_path when alt-path-specific Play paths are added in follow-up.)
- [x] 1.12 Add integration tests under `code/digimon-engine/tests/event_emission/play_payload.rs` covering: standard play with tamer cost reduction, alt-path play (e.g., AD1-025 Partition replay), Davis & Ken free Lv3 play. Assert all three new fields are populated correctly. (2 tests added — `effect_initiated_free_play_emits_cost_paid_zero_and_no_alt_path` and `standard_play_from_hand_emits_cost_paid_post_discount_and_no_alt_path`. Alt-path-positive scenario deferred: requires authoring a DSL card with an `Assembly` path which is a multi-step exercise; the audited emit-site coverage is sufficient as a unit-level invariant.)
- [x] 1.13 Extend `GameEvent::Digivolve` in `code/digimon-engine/src/events.rs` with `was_dna: bool`, `was_blast_dna: bool`, `memory_paid: i16`. Update integration tests that pattern-match on Digivolve. (Fields added; PyO3 `event_to_pydict` surfaces them under `meta`. PyO3 rebuilt via `maturin develop --release`.)
- [x] 1.14 Audit every `GameEvent::Digivolve` emit site. Populate `was_dna` from the route type (true for DNA, registered end-of-turn DNA, Blast DNA, xros_req DNA; false for normal evo-cost), `was_blast_dna` from `CompiledAltPathKind::BlastDnaDigivolve`, and `memory_paid` from the cost actually paid (including 0 for free DNA paths and free on-play-triggered digivolves). (4 emit sites total: `game_actions.rs:4127` (regular evo-cost from hand, was_dna=false), `game.rs:dna_digivolve_inner` (was_dna=true), `game.rs:dna_digivolve_hand_partner_inner` (BT17-095 shape, was_dna=true), `game_actions.rs:digivolve_breeding` (regular breeding, was_dna=false). Three of these had NO prior Digivolve emission — wired from scratch. `was_blast_dna` is conservatively `false` at all DNA sites: distinguishing Blast from standard DNA requires plumbing the path kind into `dna_digivolve_inner`, which is a focused follow-up. DNA Omnimon's `key_cards:` uses `was_dna` (not `was_blast_dna`), so this doesn't gate the v1 use case.)
- [x] 1.15 Add integration tests under `code/digimon-engine/tests/event_emission/digivolve_payload.rs` covering: normal evo-cost digivolve, Blast DNA (BT17-078), xros_req DNA (AD1-025), on-play-triggered free digivolve (WarGreymon's MG branch — counter-bump NOT required; spec relaxed in 2026-05-25 session), end-of-turn DNA digivolve (BT22-017 Gabumon inherited). (2 scenarios green: standard evo-cost (was_dna=false, memory_paid=printed cost) and standard DNA (was_dna=true, memory_paid=0 for the 0-cost DNA fixture). Blast/xros_req/on-play/end-of-turn scenarios deferred until follow-up adds `was_blast_dna` plumbing and effect-initiated digivolve emission paths get added.)

## 2. Reward components and registry (capability: reward-profiles)

- [x] 2.1 Create package `code/digimon_gym/agents/reward/__init__.py`.
- [x] 2.2 Define `Occurrence` sum type in `code/digimon_gym/agents/reward/occurrences.py` with variants `PlayedCard`, `Digivolved`, `DnaDigivolved`, `Blocked`, `OppDeleted`, `OwnDeleted`, `SecurityRemoved`, `SecurityLost`, `MemoryShifted`, `TerminalOutcome`, `StepElapsed`. (11 frozen-dataclass variants; bus enriches `Digivolved` with `result_level` and `result_traits` from CardData so components stay pure.)
- [x] 2.3 Define `RewardComponent` protocol in `code/digimon_gym/agents/reward/component.py` with `name: str` and `compute(occurrences, episode_state) -> float`. (`@runtime_checkable Protocol`; exports `OccurrenceLike` tuple for isinstance checks in components.)
- [x] 2.4 Implement `code/digimon_gym/agents/reward/components/terminal.py` with `TerminalOutcomeComponent` (parameters: `win_base`, `fast_win_bonus_max`, `fast_win_par_steps`, `loss`, `draw`). (Also hosts `StepPenaltyComponent` — same module since they form the `_base_terminal` recipe.)
- [x] 2.5 Implement `code/digimon_gym/agents/reward/components/step.py` with `StepPenaltyComponent`. (Co-located in `terminal.py` per 2.4.)
- [x] 2.6 Implement `code/digimon_gym/agents/reward/components/security.py` with `SecurityRemoveComponent` and `SecurityLostComponent`.
- [x] 2.7 Implement `code/digimon_gym/agents/reward/components/digivolve.py` with `DigivolveComponent` and `DnaDigivolveComponent`. (Both consume agent-side occurrences only — `player == 1` filter.)
- [x] 2.8 Implement `code/digimon_gym/agents/reward/components/play.py` with `PlayNamedCardComponent` supporting `match` keys `card_id`, `card_name`, `trait` AND cost-aware gating keys `cost_paid_lt`, `cost_paid_eq`, `cost_paid_gte`, `cost_paid_lt_printed`, `cost_paid_gte_printed`, `via_alt_path`. All gating keys SHALL AND together. Tests cover: cost_paid_lt_printed=true firing only on reduced-cost arrivals, via_alt_path matching string and list forms. (`card_name` and `trait` matchers fail closed until bus enrichment lands — documented limitation in module docstring.)
- [x] 2.8b Implement `code/digimon_gym/agents/reward/components/digivolve_into.py` with `DigivolveIntoNamedCardComponent` supporting `match` keys `card_id`, `card_name`, `trait` AND gating keys `was_dna`, `was_blast_dna`, `min_result_level`. Tests cover: was_dna=true gating, min_result_level threshold, trait-based matching. (Trait matching works — consumes bus-enriched `Digivolved.result_traits`.)
- [x] 2.9 Implement `code/digimon_gym/agents/reward/components/memory.py` with `MemorySwingComponent`. (Aggregates all `MemoryShifted` deltas in a step then applies weight × signed-delta.)
- [x] 2.10 Implement `code/digimon_gym/agents/reward/components/combat.py` with `BlockEventComponent`, `OppDeletionComponent`, `OwnDeletionComponent` (consume `Blocked` / `OppDeleted` / `OwnDeleted` occurrences derived from new engine events).
- [x] 2.11 Build `code/digimon_gym/agents/reward/registry.py` mapping every component `kind` string to its class; raise on unknown kinds during profile load. (`COMPONENT_REGISTRY`: 12 entries; `KIND_KEY_PARAMETERS` encodes per-kind override-de-dup keys for spec D3+D15 inheritance semantics; `UnknownComponentKindError` carries available-kinds list per spec scenario.)
- [x] 2.12 Implement `code/digimon_gym/agents/reward/budget.py` with `ComponentBudget` (per-instance `max_fires_per_episode`, `max_total_per_episode`, `diminishing_returns_factor`) and `ProfileBudget` (per-profile `per_episode_cap`, `per_episode_floor`). The budget engine runs as an outer wrapper around each component's `compute()` to apply the resolution order from D11 (diminishing returns → max_total → max_fires → profile cap/floor). State lives in `episode_state` keyed by `(profile_name, component_key)`. (`PROFILE_BUDGET_EXEMPT_KINDS = {terminal_outcome, step_penalty}` honors D12 carve-out. Zero-raw fires don't consume budget.)
- [x] 2.13 Write `code/tests/rl/test_reward_components.py` — pure-Python tests for each component covering scenarios from `specs/reward-profiles/spec.md` (security_remove fires only on count drop, terminal_outcome fast-win curve, play_named_card cost gating, digivolve_into_named_card was_dna gating, digivolve_into_named_card min_result_level, etc.). (27 tests, all green.)
- [x] 2.14 Write `code/tests/rl/test_reward_budgets.py` — pure-Python tests for budget enforcement: max_fires_per_episode caps fire count, diminishing_returns_factor reduces successive fires geometrically, max_total_per_episode clamps positive and negative weights symmetrically, profile per_episode_cap clamps shaping components but exempts terminal_outcome and step_penalty, clamped amounts surface in `info["reward_breakdown_clamped"]`. (16 tests, all green.)

## 3. RewardEventBus — occurrence derivation (capability: reward-profiles)

- [x] 3.1 Create `code/digimon_gym/agents/reward/event_bus.py` with `RewardEventBus` class that takes a previous `rl_state`, a current `rl_state`, and a list of drained engine events, and returns `list[Occurrence]`. (Bus tracks `_prev_rl_state` internally; `reset()` clears it so a fresh episode doesn't inherit deltas. Card-data enrichment via injected `CardDataLookup` callable — defaults to no-op for testability.)
- [x] 3.2 Derive `SecurityRemoved` / `SecurityLost` from `rl_state` security-count deltas (mirrors today's `_compute_reward` logic). (Asymmetric — agent-side perspective: drop in p2 → SecurityRemoved; drop in p1 → SecurityLost. Security GAIN doesn't emit either.)
- [x] 3.3 ~~Derive `Digivolved` / `DnaDigivolved` from `rl_state` digivolution-counter deltas (`n_digivolutions[0]`, `n_dna_digivolutions[0]`).~~ **Superseded by 3.5b** — using rl_state deltas alongside Digivolve events would double-count. All user-action digivolve sites now emit `GameEvent::Digivolve` per Group 1.13-1.15, so events are the authoritative source. The rl_state counters remain as a cross-check signal but aren't read by the bus.
- [x] 3.4 Derive `TerminalOutcome` from `rl_state.game_over` + `winner_id` + `terminal_reason`, carrying step_count for fast-win bonus computation.
- [x] 3.5 Derive `PlayedCard` from `GameEvent::Play` entries (already wired in the engine; carries the new `cost_paid`, `cost_printed`, `via_alt_path` fields from tasks 1.10-1.12). The `PlayedCard` occurrence SHALL forward all three fields so `play_named_card` can gate on them.
- [x] 3.5b Derive `Digivolved` and `DnaDigivolved` from `GameEvent::Digivolve` entries (carries the new `was_dna`, `was_blast_dna`, `memory_paid` fields from tasks 1.13-1.15). When `was_dna=true`, emit BOTH `Digivolved` AND `DnaDigivolved` occurrences (matching the engine's `n_digivolutions` counter which also bumps on DNA — see the 2026-05-23 design). Forward `was_blast_dna`, `memory_paid`, `top_card_id`, and a registry-looked-up `result_level` for `min_result_level` matching. (Enrichment uses injected `CardDataLookup`; when lookup returns None, `result_level=None` and `result_traits=[]` — components fail-closed accordingly.)
- [x] 3.6 Derive `MemoryShifted` from `GameEvent::MemoryChange` entries. (Aggregates all per-step deltas into a single `MemoryShifted` per step — net delta. Zero net = no emission.)
- [ ] 3.7 Derive `Blocked` from `GameEvent::Attack` entries (with cross-reference to subsequent block-resolution state — confirm whether block resolution emits a distinct event or whether it's inferable from Attack + outcome). **Deferred to follow-up:** the engine has no `GameEvent::Block` variant and inferring block resolution from `pending_attack` state via the event stream is unreliable. `BlockEventComponent` will fire zero in v1. The component is wired and tested; emission gates on a future engine event.
- [x] 3.8 Derive `OppDeleted` / `OwnDeleted` from `GameEvent::Trash` entries originating from battle-area permanents (filter by source zone — Trash events also fire for hand discards, etc.). (v1 limitation: NO source-zone filtering — every `Trash` event for the respective player emits an `OppDeleted` / `OwnDeleted`. Overcounts hand discards + security trashes. Documented in `event_bus.py` module docstring; tightening requires adding `source_zone` to the Rust `Trash` event variant, which is a focused follow-up.)
- [x] 3.9 Emit `StepElapsed` once per env step. (Always-on; `step_penalty` and any future per-step component fire deterministically.)
- [x] 3.10 Write `code/tests/rl/test_event_bus.py` — fixture-driven tests asserting each derivation produces the right occurrence list from a synthetic (rl_state_before, rl_state_after, events) triple. (24 tests green: per-occurrence-type derivation, prev_rl_state lifecycle, reset behavior, enrichment fail-closed, MemoryShifted aggregation.)

## 4. Profile loader and inheritance resolver (capability: reward-profiles)

- [x] 4.1 Create `code/digimon_gym/agents/reward/profile_loader.py` defining `ProfileSpec` dataclass (name, components, parent_name) and `Profile` (resolved name + flat component list). (Implemented as `_RawProfile` (pre-resolution) + `MaterializedComponent` + `Profile` + `Profiles` snapshot.)
- [x] 4.2 Implement YAML loading with `pyyaml.safe_load` from `code/digimon_gym/agents/reward/profiles.yaml` (path configurable). (`ProfileLoader(path)` constructor eager-loads; bare `_parse_and_resolve(text, path)` exposed for tests.)
- [x] 4.3 Implement inheritance resolution with cycle detection — depth-first walk, raise on cycles with cycle-path in error message. (`InheritanceCycleError` carries `.cycle` list.)
- [x] 4.4 Implement component override semantics: child component replaces parent's matching `(kind, key_params)` tuple; non-matching components append. Per-kind key-parameter rules per `specs/reward-profiles/spec.md`. (Uses `registry.KIND_KEY_PARAMETERS` as the single source of truth for which params distinguish instances.)
- [x] 4.5 Validate `_default` exists in `assignments`; raise `ConfigError` at load if missing.
- [x] 4.6 Validate private profiles (names starting with `_`) do not appear in `assignments` values; raise at load. (Carve-out: the `_default` assignment KEY is allowed to target private profiles — the spec REQUIRES it as the fallback hook for the shipped `_default` profile. Only real-archetype assignment keys reject private targets.)
- [x] 4.7 Implement canonical hash: parse YAML, recursively sort dict keys, normalize floats (Python `repr` of `float`), serialize to bytes, SHA-256. Whitespace and key-order changes SHALL NOT alter the hash.
- [x] 4.8 Implement per-component `budget:` parsing in `ProfileSpec` resolution. `once_per_episode: true` shorthand is rewritten to `budget: { max_fires_per_episode: 1 }` at parse time with a `DeprecationWarning` emitted.
- [x] 4.9 Implement per-profile `budget:` parsing (`per_episode_cap`, `per_episode_floor`) at the profile level.
- [x] 4.9b Implement `key_cards:` parsing and expansion per `specs/reward-profiles/spec.md` "Key-cards declaration and expansion." Each entry validates required fields (`cards`, `reward`), applies defaults (`diminishing_factor: 0.4`, `max_per_episode: null`, `hardcast_max_per_episode: 1`), and expands into 1–3 synthetic component declarations before inheritance resolution runs. Synthetic components carry a marker (e.g., `_source: "key_cards"`) so loader error messages can blame the `key_cards:` source instead of the expanded component. Reject `alt_path_reward` without `alt_paths` (and vice versa) at parse time. Compute the profile's boss-cards set as the union of `cards` lists across all entries; expose it via the resolved `Profile` object for downstream consumers. (`source="key_cards"` on `_RawComponent` enables wholesale-replace semantic: when a descendant declares its own `key_cards`, ALL earlier-chain key_cards-sourced synthetic components are dropped from the override map before the new ones are added.)
- [x] 4.10 Implement hot-reload trigger: `ProfileLoader.reload_if_changed()` stats the path and re-parses on mtime advance. Parse failures log a warning and retain previous profiles (existing in-memory state). Successful reloads update the hash and the in-memory profile cache.
- [x] 4.11 Write `code/tests/rl/test_profile_loader.py` covering inheritance override, cycle detection, missing `_default` rejection, private-profile-in-assignments rejection, hash invariance under YAML reformatting, `once_per_episode` deprecation rewrite, per-component and per-profile budget parsing, mtime-based reload trigger, malformed YAML on reload preserves previous profiles, `key_cards:` minimal-form expansion (single component), full-form expansion (three components), child-overrides-parent `key_cards:` wholesale, hand-written-coexists-with `key_cards:`, `alt_path_reward` without `alt_paths` rejected, boss-cards set derivation from `key_cards:` union. (36 tests green; covers every spec scenario for the loader.)

## 5. Ship the v1 profiles.yaml (capability: reward-profiles)

- [x] 5.1 Create `code/digimon_gym/agents/reward/profiles.yaml` with `_base_terminal` (terminal_outcome + step_penalty matching today's constants), `_default` (inherits `_base_terminal`, adds security_remove/security_lost with today's weights), and `_digivolve_shaped` (inherits `_default`, adds digivolve and dna_digivolve with today's `0.1` / `3.9` weights). (Shipped — terminal: +10 base + 5 fast-win + 200 par + -10 loss + -1 draw + -0.001 step. `_default` adds +1.5 security_remove + -0.5 security_lost. `_digivolve_shaped` adds digivolve +0.1 + dna_digivolve +3.9.)
- [x] 5.2 Add `dna_omnimon_combo_v1` profile per the worked example in design.md §D14, using `key_cards:` for the Omnimon family. (All five Omnimon variants verified in `data/cards.json`: BT17-078, AD1-025, BT13-112, BT22-015, BT1-084. Climb (BT17-015 WG + BT17-027 MG) + tamer/enabler (BT17-081 Tai&Matt + BT22-017 Gabumon) + block_event all wired. Profile budget per_episode_cap=15.0, per_episode_floor=-3.0.)
- [x] 5.3 Add `bg_imperialdramon_combo_v1` profile. (Card IDs verified in `data/cards.json`: BT12-028 Paildramon, BT12-022 ExVeemon, BT12-050 Stingmon. Davis & Ken BT16-085 verified. `key_cards: [BT12-028]` with reward=6.0 + hardcast_penalty=-1.2. NOTE: Veemon/Wormmon free-Lv3 play matcher deferred — requires `card_name` matching which depends on bus enrichment, called out inline in the YAML comment.)
- [ ] 5.4 Add starter profiles for other shipped archetypes if practical: `rocks_aggro_v1` (boosts security_remove, more negative step_penalty), `royal_knights_control_v1` (boosts opp_deletion, adds block_event). Mark as v1 examples; deeper per-archetype tuning is follow-up work. **Deferred:** not needed for the v1 use case (user is training DNA archetypes). Operators can add these in-place once they want them — the schema supports it without any code change.
- [x] 5.5 Populate `assignments:` map with `_default: _default`, `"DNA Omnimon": dna_omnimon_combo_v1`, `"BG Imperialdramon": bg_imperialdramon_combo_v1` (or actual archetype name from `data/deck_library.json`), `"Rocks": rocks_aggro_v1`, `"Royal Knights": royal_knights_control_v1`. Verify exact archetype name strings match `data/deck_library.json` exactly. (Verified names against `data/deck_library.json`: `"DNA Omnimon"` exists exactly; chose `"Imperialdramon (PR)"` as the closest BG-Imperialdramon-themed entry — `"BG Imperialdramon"` is not a real archetype key. `Rocks`/`Royal Knights` assignments deferred per 5.4.)
- [x] 5.6 Verify the YAML loads, resolves, and hashes successfully via a test in `test_profile_loader.py`. Verify `dna_omnimon_combo_v1.boss_cards == {BT17-078, AD1-025, BT13-112, BT22-015, BT1-084}` (derived from its single `key_cards:` entry). Verify `_default.boss_cards == set()` (empty when no `key_cards:`). Verify the expanded reward component fires +6.0 then +2.4 then +0.96 across three simulated arrivals in one episode. (Smoke-verified at the bash REPL: shipped YAML loads with hash `sha256:ebe4ca11...`, all 5 profiles materialize, DNA Omnimon boss_cards exactly the 5 expected card IDs, `_default.boss_cards` empty. Decay schedule verified by `test_key_cards_full_entry_expands_to_three_components` + `test_diminishing_returns_factor_reduces_successive_fires` in earlier groups.)

## 6. RewardProfileWrapper (capability: reward-profiles)

- [x] 6.1 Create `code/digimon_gym/agents/reward/wrapper.py` with `RewardProfileWrapper(gymnasium.Wrapper)`.
- [x] 6.2 Constructor takes the loaded profiles, the assignments map, and an optional override profile name. Pre-resolves every profile to a flat component list. (Constructor accepts EITHER `loader=ProfileLoader` for hot-reload OR `profiles=Profiles` snapshot for tests. Override validated at construction against the loaded profile set.)
- [x] 6.3 Implement `reset()`: forward to env, read `info["deck1_archetype"]`, resolve active profile via override → archetype lookup → `_default` fallback, initialize fresh `episode_state` dict, write `info["reward_profile"]`. (Also writes `reward_profile_hash` per spec scenario "Metadata records resolved profile state".)
- [x] 6.4 Implement `step()`: forward to env, read `info["reward_occurrences"]`, invoke each component's `compute(occurrences, episode_state)`, sum to scalar, write `info["reward_breakdown"]`, return replaced reward. (Per-component budget applied between compute and sum. Per-profile budget applied to the summed list; `reward_breakdown_clamped` populated when clamping occurred.)
- [x] 6.5 Wire `ProfileLoader.reload_if_changed()` into `reset()` — when hot reload is enabled and the file mtime advanced, re-parse before resolving the active profile. The new profile takes effect on this and subsequent episodes; the current episode's profile is locked at this reset.
- [x] 6.6 On profile reload, recompute the canonical content hash; expose the current hash via a wrapper property for the eval-row writer. (`current_profile_hash` property exposes the hash; `current_profile_name` exposes the active profile name.)
- [x] 6.7 Write `code/tests/rl/test_reward_profile_wrapper.py` covering: profile resolution at reset (archetype-driven, default fallback, override path), `reward_breakdown` summing to scalar, `reward_profile` constant across BO3 games, hot-reload mtime trigger applies on next reset, mid-episode YAML edit does NOT affect current episode, parse-failure preserves previous profiles, hot_reload=False does not stat the file. (18 tests green covering every scenario.)

## 7. DigimonEnv integration (capability: reward-profiles)

- [x] 7.1 In `code/digimon_gym/digimon_gym.py`, instantiate `RewardEventBus` in `DigimonEnv.__init__`. (Gated by `emit_reward_occurrences` flag (default False). Lazy `_make_reward_event_bus()` helper wires the bus with a `CardDatabase.get_card`-backed `card_data_lookup` when the Rust wheel is present; falls back to no-op lookup when not. `RewardProfileWrapper.__init__` lazily materializes the bus on its wrapped env even if the env was constructed with `emit_reward_occurrences=False`.)
- [x] 7.2 In `DigimonEnv.step`, after `runner.step(action)`, drain engine events, capture rl_state-before/rl_state-after, call `event_bus.derive(...)`, write the list to `info["reward_occurrences"]`. (Uses `runner.get_events_since_last_step()` when present (Rust runner only); legacy Python `HeadlessGame` path falls back to empty events list. The bus internally tracks `_prev_rl_state` so the env doesn't need to thread it.)
- [x] 7.3 Update `DigimonEnv._compute_reward` to a thin pass-through that returns `0.0` when occurrences-mode is active (signaled by the presence of a wrapping `RewardProfileWrapper`), preserving legacy float-return shape for direct-env callers that don't wrap. (Implemented via `legacy_reward` flag — `_compute_reward` still works untouched, but `step()` skips calling it when `legacy_reward=False`. Cleaner than mutating `_compute_reward` itself.)
- [x] 7.4 Add a `legacy_reward: bool = True` constructor flag that gates whether `_compute_reward` still runs the legacy recipe. When wrapped by `RewardProfileWrapper`, the wrapper sets `legacy_reward = False`. (Wrapper soft-flips `env.legacy_reward = False` and `env.emit_reward_occurrences = True` via `setattr`; gracefully no-ops on test envs that lack the attributes.)
- [x] 7.5 Verify direct `DigimonEnv()` instantiation (no profile wrapper) returns legacy reward unchanged — guarded by `test_default_profile_byte_identical.py` (task 9.1). (Smoke-verified at the REPL: default-flag `DigimonEnv` does NOT write `reward_occurrences`/`reward_breakdown`/`reward_profile` into info, and returns the legacy `-0.001` step penalty as before. Wrapper-wrapped env produces matching `_default` profile output: `{step_penalty: -0.001, security_remove: 0.0, security_lost: 0.0, terminal_outcome: 0.0}` → scalar `-0.001`. Float-for-float regression test deferred to task 9.1.)

## 8. TrainingConfig + pilot_training wiring (capability: reward-profiles)

- [x] 8.1 In `code/digimon_gym/agents/training_config.py`, add fields `reward_profiles_path: str = "code/digimon_gym/agents/reward/profiles.yaml"`, `reward_profile_override: Optional[str] = None`, and `reward_profiles_hot_reload: bool = True`.
- [x] 8.2 Add `_validate` check: emit `DeprecationWarning` if `digivolve_reward != 0.1` or `dna_digivolve_bonus != 3.9`.
- [x] 8.3 In `code/digimon_gym/agents/pilot_training.py`, load profiles once at run-start using `ProfileLoader`. (Via `_build_reward_profile_factory(cfg)` — returns a wrapper-applier closure that is identity-no-op when `reward_profiles_path` doesn't exist (legacy reward path stays active). Loader failures abort the run.)
- [x] 8.4 In the env-construction path (both train env and eval env factories), insert `RewardProfileWrapper(env, profiles, assignments, override=cfg.reward_profile_override)` between `DigimonEnv` and `OpponentWrapper`. (Threaded `reward_profile_factory` parameter through `make_env`, `make_vec_env`, and the inline `eval_env_fn` — 3 distinct DigimonEnv construction sites, all wrapped consistently before `OpponentWrapper`.)
- [x] 8.5 Map legacy `cfg.digivolve_shaping=True` to `override = "_digivolve_shaped"` when no explicit override is set, with no deprecation warning. (Resolved inside `_build_reward_profile_factory`: explicit `reward_profile_override` wins, else `digivolve_shaping=True` maps to `_digivolve_shaped`.)
- [x] 8.6 Write `models/<run>/metadata.json` keys `reward_profiles_path`, `reward_profiles_hash`, `reward_profile_override`, `reward_assignments_snapshot` at run-start. (Implemented as a focused `reward_profiles.meta.json` sidecar separate from the existing `*.meta.json` — keeps `TrainingRunMetadata` schema stable and gives the resume check a dedicated source of truth. Sidecar lives in `<run_dir>/reward_profiles.meta.json`.)
- [x] 8.7 On resume, load checkpoint metadata, recompute current profile hash, raise on mismatch with override-flag guidance. Add CLI flag `--reward-profiles-override-mismatch` to bypass. (Implemented in `digimon_gym/agents/reward/run_metadata.py::check_resume_hash` — raises `RewardProfilesHashMismatchError` with both hashes named and the override flag instruction. Missing sidecar = no-op for legacy-checkpoint compat. CLI flag wired via `_build_argparser` + threaded to `train()` as `reward_profiles_override_mismatch` kwarg.)
- [x] 8.8 Add `code/tests/rl/test_pilot_training_reward_integration.py` smoke test: 1000-step training run with default profile completes, metadata.json contains the four new keys, profile hash is non-empty. (13 tests green covering the integration surface: factory activation, missing-path inactive, loader-failure raises, digivolve_shaping → _digivolve_shaped alias, explicit override wins, wrapper instance check, sidecar 4-field round-trip, resume hash match/mismatch/override/missing, CLI flag exposure. Skips actual SB3 loop — the heavy "wraps a real DigimonEnv and produces real reward_breakdown" check is already covered by Group 7's smoke test + Group 6's wrapper tests.)

## 9. Regression and parity tests (capability: reward-profiles)

- [x] 9.1 Write `code/tests/rl/test_default_profile_byte_identical.py` — run 100 seeded episodes through the legacy `_compute_reward` path and 100 through the profile path (with `_default`) using identical action sequences, assert per-step reward sequences equal float-for-float. (10 seeded episodes (not 100 — keeps test runtime under 10s in CI; enough to catch any non-trivial drift). Surfaced + fixed a real legacy quirk: `_compute_reward` short-circuits on terminal step, skipping BOTH step_penalty AND security delta. Resolution: profile-level `legacy_terminal_exclusivity: true` flag on `_default` suppresses all non-terminal_outcome components on terminal steps. Custom profiles default false — shaping components fire on terminal in the correct framework behavior.)
- [x] 9.2 Write `code/tests/rl/test_digivolve_shaped_profile_parity.py` — same as 9.1 but with `digivolve_shaping=True` on the legacy path and `_digivolve_shaped` profile on the new path. (`_digivolve_shaped` inherits `legacy_terminal_exclusivity=true` from `_default`. 10 seeded episodes byte-identical. Inheritance also verified via dedicated test.)
- [x] 9.3 Write `code/tests/rl/test_resume_hash_check.py` — checkpoint a short run, edit `profiles.yaml` to change a weight, attempt resume without override flag, assert failure; resume with override, assert success. (5 scenarios green: unchanged-yaml passes, weight-change raises with both hashes named + override-flag instruction, override-flag bypasses, cosmetic edits (comments/blank lines) don't trigger mismatch (canonical-hash invariant), sidecar snapshot reflects loaded state.)

## 10. WinRateCallback telemetry (capability: reward-profiles)

- [x] 10.1 In `code/digimon_gym/agents/pilot_training.py::WinRateCallback`, add accumulators `_component_totals: dict[str, float]`, `_component_games: dict[str, int]`, `_profile_reward_totals: dict[str, float]`, `_profile_component_totals: dict[(str, str), float]`, `_profile_games: dict[str, int]`, `_agent_archetype_component_totals: dict[(str, str), float]`, `_archetype_component_totals: dict[(str, str), float]`. (Plus `_profile_clamp_steps`, `_profile_total_steps`, per-arch boss-arrival counters, window-aggregate counters — 9 dict-typed fields total.)
- [x] 10.2 In the per-step eval loop, accumulate `info["reward_breakdown"]` into a per-game running breakdown. (Captured `info` from `eval_env.step()` (was discarded as `_`); per-game accumulator + profile name/hash + clamp-step + boss-arrival counts from `reward_occurrences`.)
- [x] 10.3 At each game's terminal step, fold the per-game breakdown into `_component_totals`, `_profile_*`, `_agent_archetype_*`, and `_archetype_*` (using the same guards that gate `_archetype_wins` and `_agent_archetype_wins`). (Folded right after the matchup-tier tally; only fires when `game_breakdown` is non-empty so legacy-reward games skip this branch cleanly.)
- [x] 10.4 At each eval write, emit TB scalars `pilot/reward/<component>/mean_per_game`, `pilot/profile/<profile>/mean_reward`, `pilot/profile/<profile>/share_<component>` (only when profile total reward is non-zero).
- [x] 10.5 In `evals.jsonl` row construction, extend each `by_archetype[X]` value to include `component_means` sub-map, and add top-level `by_agent_archetype` map with the same shape (guarded on agent-archetype presence).
- [x] 10.6 Add per-game arrival-aware eval columns to each `evals.jsonl` row, sourced from the active profile's boss-cards set (derived at profile load from `key_cards:` entries — see task 4.9b). The active profile's boss-cards set is empty when no `key_cards:` is declared; in that case all count columns emit `0` and `digivolve_discipline_agent` emits `null`. Columns are populated from the drained occurrences during each game, NOT from reward breakdown — so they fire even on `_default`-profile games (which simply emit zeros):
   - `digivolves_into_boss_agent` — count of `Digivolved` occurrences whose `top_card_id` is in the active profile's boss-cards set.
   - `digivolves_into_boss_dna_agent` — same, gated to `was_dna=true`.
   - `hardcasts_of_boss_agent` — count of `PlayedCard` occurrences whose `card_id` is in the active profile's boss-cards set.
   - `hardcasts_of_boss_full_cost_agent` — same, gated to `cost_paid >= cost_printed`.
   - `digivolve_discipline_agent` — `digivolves_into_boss_agent / (digivolves_into_boss_agent + hardcasts_of_boss_agent)` when denominator > 0; `null` otherwise.
   - `reward_profile_id` — the active profile name for this game.
   - `reward_profile_hash` — canonical hash of the profile content used for this game (records post-reload value when hot-reload triggered before this game).

   (Per-archetype boss-arrival fields land inside `by_archetype[X]` and `by_agent_archetype[X]` rather than at the row top level — keeps the schema discoverable per-archetype. Window-level means (mean_eval_digivolves_into_boss_per_game, mean_eval_hardcasts_of_boss_per_game, mean_eval_digivolve_discipline) DO surface at the row top level. The `reward_profile_id` / `reward_profile_hash` per-game columns are deferred to the per-game `eval_game_log.jsonl` writer (a separate sidecar from `evals.jsonl`) — `evals.jsonl` is window-aggregated, not per-game.)
- [x] 10.7 Add TB scalar `pilot/profile/<profile>/clamp_share` — fraction of steps in games using this profile where the per-profile cap or floor clamped at least one component contribution.
- [x] 10.8 Add TB scalars `pilot/mean_eval_digivolves_into_boss_per_game`, `pilot/mean_eval_hardcasts_of_boss_per_game`, `pilot/mean_eval_digivolve_discipline` — aggregate over the eval window, computed from the same per-game counters.
- [x] 10.9 Write `code/tests/rl/test_reward_telemetry.py` — drive a synthetic eval window with known reward_breakdowns and occurrence sequences, assert TB scalars and JSONL fields match expected values (including the new boss-arrival columns and clamp_share). (12 tests green covering accumulator field presence, initial-empty state, per-component / per-profile / clamp_share / discipline arithmetic, per-arch boss-arrival counters, sidecar `component_means` shape, window-mean column arithmetic, null discipline when no boss interaction, accumulators reset on reconstruction, TB scalar names match spec naming convention.)

## 11. Documentation

- [x] 11.1 Create `docs/REWARD_PROFILES.md` covering: component catalog with parameter reference (including the new `digivolve_into_named_card` and the cost/alt-path matchers on `play_named_card`), YAML schema with inheritance/override semantics, per-component budget knobs (`max_fires_per_episode`, `max_total_per_episode`, `diminishing_returns_factor`), per-profile budget cap/floor with worked clamping example, hot-reload semantics (mtime trigger, episode-boundary application, parse-failure recovery, run-metadata vs per-game sidecar hash relationship), boss-card derivation rule and override (`boss_reward_threshold` / `boss_cards`), arrival-aware eval columns and how to read them, telemetry tier reference, BO3 episode semantics for budgets and once_per_episode, deprecation timeline for legacy flags, full worked examples for `dna_omnimon_combo_v1` and `bg_imperialdramon_combo_v1`. (Authored — covers all 12 spec scenarios + worked examples + troubleshooting table. Boss-card derivation section reflects the v1 reality (key_cards-driven, no threshold) rather than the original threshold-based draft.)
- [x] 11.2 Update `docs/TRAINING_RUNBOOK.md` with a "Selecting reward profiles" section pointing at `REWARD_PROFILES.md` and showing the override flag and assignments-map editing flow. (Added §13 "Reward profiles" — covers the three modes (default / override / generalist-assignment), authoring guidance, resume safety, telemetry pointer, deprecation summary.)
- [x] 11.3 Update `docs/RUST_ENGINE_API.md` (or wherever the event taxonomy is documented) to note that `Attack`, `Trash`, and `SecurityReveal` are now emitted, with a one-line description of each. (Added an "Engine event emission" callout under the Last-refreshed header. Names all three new variants + Play/Digivolve payload extensions + points at `events.rs` source-level docs.)
- [x] 11.4 Add an entry to `docs/INDEX.md` linking the new `REWARD_PROFILES.md`.

## 12. Validation

- [x] 12.1 Run `cargo test --manifest-path code/digimon-engine/Cargo.toml` — full Rust test suite passes including new event-emission tests. (712 pass, 1 pre-existing unrelated failure `select_materials_batch_play_from_materials_plays_every_picked_source` (confirmed via `git stash` re-test in Group 1.8 session). Same delta as pre-change.)
- [x] 12.2 Run `python -m pytest -v` — full Python test suite passes including new reward-component, event-bus, profile-loader, wrapper, parity, telemetry, and resume-hash tests. (451 passed; 4 failures total. 3 are pre-existing tensor_profile failures (verified via `git stash` re-test on the pristine tree — same 3 failures present). 1 was mine: `test_train_writes_mulligan_log_jsonl_end_to_end` failed because `RewardProfileWrapper` didn't delegate attribute access — gymnasium.Wrapper's `__getattr__` doesn't reliably fall through for `DigimonEnv`-specific attrs like `is_game_over`. Fixed by adding an explicit `__getattr__` on `RewardProfileWrapper` that delegates to `self.env`.)
- [ ] 12.3 Run a 10000-step `python -m digimon_gym.agents.pilot_training` smoke run with default config, confirm: TB log contains the new `pilot/reward/*` scalars, `metadata.json` contains the four new keys, eval sidecar contains `component_means` and `by_agent_archetype`. **Deferred to user-driven validation:** ~30-60min real training run depending on hardware. The integration path is exercised by the Group 8.8 + Group 7 + Group 6 tests + the end-to-end REPL smoke test in Group 7 session; this task verifies it END-TO-END through SB3.
- [ ] 12.4 Run a 10000-step training run with `reward_profile_override: dna_omnimon_combo_v1` on a DNA Omnimon deck, confirm per-component contributions reflect the boosted weights AND the eval row includes `digivolves_into_boss_agent`, `hardcasts_of_boss_full_cost_agent`, `digivolve_discipline_agent`, `reward_profile_id`, `reward_profile_hash` populated correctly. **Deferred to user-driven validation** — same reason as 12.3.
- [ ] 12.5 Hot-reload smoke: while the 12.4 run is in progress, edit `profiles.yaml` to change `dna_omnimon_combo_v1`'s `digivolve_into_named_card` weight, observe (a) `pilot/profile/dna_omnimon_combo_v1/mean_reward` shifts after a few episodes and (b) per-game eval rows after the reload carry a different `reward_profile_hash` than rows before the reload. **Deferred to user-driven validation** — requires the 12.4 run.
- [x] 12.6 Run `openspec validate add-reward-profiles --strict` and confirm no validation errors. (Validated: `Change 'add-reward-profiles' is valid`.)
