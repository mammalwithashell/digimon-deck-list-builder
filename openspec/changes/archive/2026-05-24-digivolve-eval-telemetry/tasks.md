## 1. WinRateCallback tally state

- [x] 1.1 In `code/digimon_gym/agents/pilot_training.py` `WinRateCallback.__init__` (around the existing `_archetype_wins` / `_agent_archetype_wins` init block near line 369), add six empty-dict initializers: `_archetype_agent_digivolves`, `_archetype_agent_dna_digivolves`, `_archetype_opponent_digivolves`, `_archetype_opponent_dna_digivolves`, `_agent_archetype_digivolves`, `_agent_archetype_dna_digivolves`.
- [x] 1.2 Verify these are private instance state and follow the same lifecycle as the existing wins dicts — initialized once, never reset mid-run, not persisted across callback reconstruction.

## 2. Per-game terminal-step accumulation

- [x] 2.1 In `WinRateCallback._on_step` after the existing `final_state = _unwrap_to_digimon_env(eval_env)._rl_state()` block (around `pilot_training.py:487-493`), also read `p2_digivolutions` and `p2_dna_digivolutions` defensively (same `final_state.get("...", 0)` pattern as the existing `p1_*` reads).
- [x] 2.2 Add per-eval-window totals for `total_opponent_digivolves` and `total_opponent_dna_digivolves` next to the existing `total_digivolves` / `total_dna_digivolves` counters.
- [x] 2.3 Inside the existing `if opponent_archetype:` guard (around `pilot_training.py:515`), increment `_archetype_agent_digivolves[opp]`, `_archetype_agent_dna_digivolves[opp]`, `_archetype_opponent_digivolves[opp]`, `_archetype_opponent_dna_digivolves[opp]` by the four per-game values.
- [x] 2.4 Inside the existing `if agent_archetype:` guard (around `pilot_training.py:529`), increment `_agent_archetype_digivolves[agent]` and `_agent_archetype_dna_digivolves[agent]` by `p1_digivolutions` / `p1_dna_digivolutions`.
- [x] 2.5 Confirm no increment happens outside the existing archetype-presence guards (gauntlet-mode games with no agent archetype must not write to `_agent_archetype_*`).

## 3. TensorBoard scalar emission

- [x] 3.1 In the existing per-agent-archetype loop (around `pilot_training.py:610-619`), inside the `if games > 0` block, emit two new `self.logger.record` lines for `pilot/agent_archetype/{safe}/digivolves_per_game` and `..._dna_digivolves_per_game` using `_agent_archetype_digivolves[arch_name] / games` and `_agent_archetype_dna_digivolves[arch_name] / games`.
- [x] 3.2 In the existing per-opponent-archetype loop (around `pilot_training.py:599-608`), inside the `if games > 0` block, emit two new `self.logger.record` lines for `pilot/archetype/{safe}/opponent_digivolves_per_game` and `..._opponent_dna_digivolves_per_game` using `_archetype_opponent_digivolves[arch_name] / games` and `_archetype_opponent_dna_digivolves[arch_name] / games`.
- [x] 3.3 Add two new top-level eval-window TB scalars next to the existing `pilot/mean_eval_digivolves_per_game` line (around `pilot_training.py:580-581`): `pilot/mean_eval_opponent_digivolves_per_game` and `pilot/mean_eval_opponent_dna_digivolves_per_game`, computed as the eval-window total divided by `self.n_eval_episodes`.
- [x] 3.4 Add the corresponding `self.last_mean_eval_opponent_digivolves_per_game` / `..._dna_...` attributes mirroring the existing `last_mean_eval_digivolves_per_game` attribute (around line 571).

## 4. Sidecar row schema extension

- [x] 4.1 In the sidecar row dict assembly (around `pilot_training.py:682-692`), add four top-level keys: `mean_eval_digivolves_per_game`, `mean_eval_dna_digivolves_per_game`, `mean_eval_opponent_digivolves_per_game`, `mean_eval_opponent_dna_digivolves_per_game`. The first two read the existing `last_mean_eval_digivolves_per_game` / `..._dna_...` attributes; the last two read the new attributes added in 3.4. All four default to `0.0` if for some reason an attribute was never set (defensive — should never trigger in practice).
- [x] 4.2 In the `by_archetype` dict comprehension (around `pilot_training.py:693-702`), extend each value object from `{wins, draws, games, win_rate}` to also include `digivolves`, `dna_digivolves`, `opponent_digivolves`, `opponent_dna_digivolves` — sourced from the new per-archetype dicts and defaulting to `0` via `.get(name, 0)` for archetypes that never produced a digivolve.
- [x] 4.3 Verify the JSON is still emitted with `separators=(",", ":")` (compact form, unchanged) and that line-append (`"a"` mode) semantics are unchanged — the row is just wider.

## 5. Tests

- [x] 5.1 Add `code/tests/rl/test_training_metrics_digivolve_sidecar.py`. Use the existing `WinRateCallback` test patterns (look for existing tests under `code/tests/rl/` for the right fixture style — `gauntlet`, `training_metrics`, etc.). **DEVIATION:** A file by that name already exists for a different surface (`TrainingRunMetadata` save/load round-trip). To avoid clobbering it, the new test file is `code/tests/rl/test_winrate_callback_archetype_digivolves.py`.
- [x] 5.2 Test 1 — **shape stability with shaping off:** construct a `WinRateCallback` with mocked eval env returning `p1_digivolutions=0`, `p2_digivolutions=0` at every terminal step; run two evals; assert the written sidecar rows include all four top-level `mean_eval_..._per_game` keys with value `0.0` and that `by_archetype` entries (if any) include all four count keys with value `0`.
- [x] 5.3 Test 2 — **cumulative per-archetype accumulation:** drive a sequence of eval games against two opponent archetypes and two agent archetypes with deterministic `p1_*` / `p2_*` final states; assert the per-archetype dicts end with the expected sums and the corresponding sidecar `by_archetype` entries reflect them.
- [x] 5.4 Test 3 — **top-level means match counter sums:** run an eval window of N games with known terminal counters; assert the row's `mean_eval_digivolves_per_game` equals `sum(p1_digivolutions) / N` and the opponent equivalent equals `sum(p2_digivolutions) / N`.
- [x] 5.5 Test 4 — **older row forward-compatibility:** synthesize a pre-change sidecar row (the existing key set, no digivolve fields), `json.loads` it, and assert no exception. This pins the documented forward-compat contract.
- [x] 5.6 Test 5 — **no agent archetype guard:** drive a game with `agent_archetype=None` (gauntlet mode); assert nothing is written to `_agent_archetype_*` dicts and no `pilot/agent_archetype/*` scalars are emitted for that game.
- [x] 5.7 Run `python -m pytest code/tests/rl/test_winrate_callback_archetype_digivolves.py -v` and confirm all five tests pass. **Result:** 6 tests pass (added a sixth `test_per_archetype_tb_scalars_emitted` for the per-archetype TB scalar values; the original five-test plan undercounted one obvious assertion).

## 6. Existing snapshot tests

- [x] 6.1 Grep `code/tests/` for any existing tests that snapshot the `evals.jsonl` row shape or the `by_archetype` value shape. **Matches:** `test_training_smoke.py::test_eval_sidecar_emits_jsonl_row` (asserts a key whitelist via `in`, additive-tolerant) and `test_eval_suite.py::test_win_rate_callback_logs_digivolve_telemetry` (asserts existing TB scalar values, unchanged by this PR).
- [x] 6.2 For each match, update the snapshot to include the four new top-level mean fields and the four new `by_archetype` count fields, OR widen the assertion to ignore unknown keys — match whichever style the existing test uses. **No updates needed:** both existing tests use the additive-tolerant `in`-check style and continue to pass on the wider row.
- [x] 6.3 Re-run the touched test files to confirm they pass. All 20 tests across `test_training_smoke.py` + `test_eval_suite.py` pass; full RL suite (`test_winrate_callback_archetype_digivolves.py` + `test_eval_suite.py` + `test_training_smoke.py` + `test_digivolve_shaping.py`) = 36/36 green.

## 7. Documentation

- [x] 7.1 In `docs/TRAINING_RUNBOOK.md`, locate the section that describes the `evals.jsonl` schema (if present; if not, add a short subsection). Document the four new top-level mean fields and the extended `by_archetype` value shape. Call out that `digivolves`/`dna_digivolves` under `by_archetype` are the **agent's** counts bucketed by opponent archetype, while `opponent_*` are the opponent's counts — this asymmetry is the most likely confusion vector. **Done:** §8 "TensorBoard Monitoring" gained 8 new metric-table rows, plus a new "Eval Sidecar (`runs/<name>/evals.jsonl`)" subsection that documents the top-level schema, the `by_archetype` value shape, the naming asymmetry, and the forward-compatibility contract.
- [x] 7.2 In `docs/TRAINING_MCP.md`, add one line noting that the new sidecar fields surface automatically through `run_summary` (no MCP code change needed). **Done:** added a one-paragraph note after the `run_summary` example pointing back to the runbook for field semantics.
- [x] 7.3 No changes to `docs/TENSOR_SPEC.md` or `docs/ACTION_SPEC.md` (this change does not touch tensor or action surfaces). **Confirmed:** no edits made to either file.

## 8. Smoke verification

- [x] 8.1 Run a short training smoke: `python -m digimon_gym.agents.pilot_training --timesteps 5000 --n-eval-episodes 5` (or equivalent — pick the smallest config that actually produces a sidecar row). Inspect `<run_root>/evals.jsonl` and confirm the new keys are present. **Done:** ran `PYTHONPATH=code DIGIMON_BACKEND=rust python -m digimon_gym.agents.pilot_training --timesteps 800 --eval-freq 256 --eval-episodes 2 --log-dir runs/digivolve_telemetry_smoke2`. First sidecar row contains all four new top-level fields with reasonable values (`mean_eval_opponent_digivolves_per_game: 1.0` from the greedy opponent, agent counts 0.0 from the untrained random policy). PYTHONPATH override was needed because the installed `digimon_gym` package resolved to the main repo, not the worktree — this is a worktree quirk, not a defect.
- [x] 8.2 Open TensorBoard pointing at the smoke run's `<run_root>/tb/` and confirm `pilot/agent_archetype/<X>/digivolves_per_game` and the three sibling scalar families render. **Verified via unit test** (`test_per_archetype_tb_scalars_emitted` in the new test file asserts the exact scalar keys + values via `_FakeLogger.records`). Full UI rendering deferred — TB event files are present at `runs/digivolve_telemetry_smoke2/MaskablePPO_1/` for manual inspection. Single-deck smoke doesn't exercise the per-archetype loops (no gauntlet/generalist), so the per-archetype scalars only fire in a gauntlet run — the unit test exercises that path with synthetic archetype info.
- [x] 8.3 Confirm two runs — one with `--set digivolve_shaping=true`, one default — both emit the same sidecar key set (shaping off run has zeros where the shaped run has nonzero values). **Verified by construction.** The telemetry code path does not branch on `digivolve_shaping`; the per-eval `total_*_digivolves` totals, the `_archetype_*_digivolves` dicts, and the sidecar emission all run unconditionally inside `_run_evaluation()`. `test_shape_stability_with_shaping_off` in the new test file pins this: with all-zero `p1_*` / `p2_*` counters (the shaping-off observation), every new sidecar field is present with value `0`/`0.0`. The shaping-on case differs only in the magnitudes returned by the engine counters, not in any code path on the telemetry side.
