## 1. Gap 2 — Digi-Egg routing on return-to-deck (smallest, isolated) — DONE 2026-05-29

- [x] 1.1 Confirmed the failing test (was `#[ignore]`-d as proven-failing): Q22 digitama empty pre-fix.
- [x] 1.2 Added private `EffectContext::move_card_to_deck(card, to_bottom)` (`effect_context/mod.rs`) routing `CardKind::DigiEgg` → `digitama_deck` (bottom = `insert(0)`, top = `push`), everything else → `deck`. Returns to the card's OWNER's deck.
- [x] 1.3 Re-pointed all four trash→deck movers through it: `return_all_trash_to_deck_bottom`, `return_trash_cards_to_deck_bottom`, `return_trash_cards_to_deck_top`, `move_trash_card_to_deck_top`. Permanent-stack returns (`Game::return_to_deck`/`return_stack_to_deck`) + reveal-zone returns share the rule but live on a separate game.rs path — noted as audit-pending in the helper doc (out of scope for this trash→deck fix).
- [x] 1.4 The `moved`/`handles` Vec is unchanged (built before the move), so dependent costs (Medusamon "return 2") stay satisfied.
- [x] 1.5 Un-ignored Q22 (`q22_digi_egg_returned_to_deck_bottom_routes_to_digitama_deck`); added the deck-TOP regression `digi_egg_returned_to_deck_top_routes_to_digitama_deck`; both pass.
- [x] 1.6 `cargo test --test judge_quiz` (7 pass / 29 ignored), `--test deletion_batching` (7 pass), `--test dsl zone_movement`/`return` (green) — no regressions.

## 2. Gap 1 — general state-based ≤0-DP rules-check (highest impact) — DONE 2026-05-29

- [x] 2.1 Confirmed the failing probe pre-fix; `..._healthy_digimon_survives_drain` passed.
- [x] 2.2 Promoted `run_rule_check_after_arts` → `Game::run_state_based_rules_check` (game_actions.rs): single pass deleting every battle-area Digimon with `effective_dp ≤ 0` via `delete_permanent_with_effects` (the batched-deletion flow), returning whether it deleted; the fixpoint loop lives in the drain wrapper (bounded by `MAX_CHAIN_DEPTH`; idempotent on already-gone handles). **Skips transiently-empty (zombie) slots** — `card_sources.is_empty()` guard — so it never reads `top_card()` on a mid-cleanup permanent.
- [x] 2.3 Invoked at top-level resolution boundaries via `drain_effect_queue`'s outermost-drain wrapper: BETWEEN each top-level queued effect (`rules_check_between_queued_effects`, after each `run_queued_effect`) AND a final fixpoint when the queue empties. Re-entrancy guarded by `Game::effect_drain_depth` (== 1 ⇒ outermost). Arts call site kept (renamed; runs the single pass before the WhenDigivolving branch). **Discovered + removed the unfaithful inline mid-effect deletion in `EffectContext::add_modifier`** (the "latent 17-1-2-2 timing bug") — deletion is now a state-based action, not mid-effect.
- [x] 2.4 Never runs mid-effect: the depth guard skips re-entrant drains (effect bodies, batched-deletion deferred drain) and parked selections; the between-effects site runs only after a top-level effect fully resolves. Pinned by the Q6-analog test (reduce-then-restore within one effect → VICTIM survives).
- [x] 2.5 Un-ignored `zero_dp_probe_reduced_digimon_deleted_after_effect_resolves` — passes.
- [x] 2.6 Added synthetic regression tests in `tests/judge_quiz/b_deferred_rules_check.rs`: (a) `q6_analog_no_mid_effect_deletion_within_single_effect` (no mid-effect deletion within one resolving effect), (b) `q24_analog_rules_check_deletes_between_queued_effects` (a Digimon driven ≤0 by a turn-player effect is deleted before a separate opponent "+DP save" trigger resolves — pins the D3 between-effects interleave). Both pass.
- [x] 2.7 Hot-path regression gate: full `cargo test --features dsl-yaml-loader --no-fail-fast` run twice (post-fix) — real test-failure set is **identical to baseline** (the 17 pre-existing failures; `combat`, `option_flow`, `deletion_batching` all green). No new failures, no double-deletion, no zombie 0-DP permanents. (RL smoke deferred — engine `option_flow`/`combat` green covers the action-surface paths; no action-space/tensor change.)

## 3. Gap 3 — defer inherited on-trash triggers (spike-gated, riskiest) — SPLIT OUT 2026-05-29

- [x] 3.1 **Calibration spike DONE.** Read EX10-036 (`cards/ex10/EX10-036.yaml`) + its behavioral test `ex10_036_clause_a_after_source_trash_prompts_opp_field_delete` (`tests/cards_behavioral/ex10/ex10_036.rs:433`). **Finding:** the synchronous `drain_effect_queue()` in `fire_digivolution_card_trashed` (game_actions.rs:4115) is load-bearing for a reason ORTHOGONAL to the on-trash observer: when Clause A's cost trashes its first source, the synchronous drain lets **Clause B — a SEPARATE already-queued WhenDigivolving effect of the same card** — fire and pick up the just-trashed Mineral cards (Clause A's inner tail parks as `dsl_outer_tail`). ~20 EX10-036 tests cover this interleave. The synchrony is about draining *already-queued sibling effects*, NOT about the `OnDigivolutionCardTrashed` observer the same call enqueues.
- [x] 3.6 **Decision: SPLIT** (per design D5 escape hatch). Q23's fix requires the inherited `OnDigivolutionCardTrashed` observer (Tumblemon's "gain 1 memory") to DEFER past a *later separate effect* (Medusamon's return-2) and re-check remain-in-trash. A remain-in-trash gate alone does NOT fix Q23 — all 3 Tumblemon are still in trash at synchronous-fire time, so the observer must genuinely defer its resolution, not just guard it. Cleanly separating "drain already-queued sibling effects (synchronous — EX10-036)" from "inherited on-trash observer (deferred + remain-in-trash re-check — Q23)" is a deep change to the trash-observer dispatch/provenance model, it risks EX10-036's ~20 tests, and it **cannot be validated without Q23's cards** (Proganomon EX8-051 / Medusamon BT24-017 full chain — Q23 CANDIDATE, Q21 BLOCKED-CARD on BT7-069/BT3-109). Therefore gap 3 is deferred to its own follow-up change; `G-ON-TRASH-OBSERVER-SYNCHRONOUS` stays OPEN in `qa/archetype-qa/engine-gaps.md` with this spike analysis. Q21/Q23 stay BLOCKED on it.
- [~] 3.2–3.5 Deferred to the gap-3 follow-up change (not implemented here).

## 4. Reconcile and verify

- [x] 4.1 Moved `G-RETURN-TRASH-DIGI-EGG-ROUTING` (Gap 2) and `G-NO-GENERAL-ZERO-DP-RULES-CHECK` (Gap 1) to `qa/resolved-gaps.md` with resolution notes + tests. `G-ON-TRASH-OBSERVER-SYNCHRONOUS` (Gap 3) stays OPEN in `engine-gaps.md` with the spike-conclusion / split note (§3.6).
- [x] 4.2 Updated `qa/qa-reports/judge-quiz.md`: Q22 → PASS (Gap 2); §0 `G-NO-GENERAL-ZERO-DP-RULES-CHECK` → RESOLVED (cluster-B per-card scenarios Q6/Q8/Q13/Q14/Q24 flip to PASS as their cards are authored); §0b on-trash carries the spike/split note.
- [x] 4.3 No judge-quiz test carries an `#[ignore]` for a gap THIS change closed: the ≤0-DP probe is un-ignored and passes; the Q6/Q8/Q13/Q14/Q24 stubs remain `#[ignore]`-d on their CARD blocks (not the engine gap); the Gap-3 (Q21/Q23) tests legitimately stay blocked on the split-out gap.
- [x] 4.4 Full `cargo test --manifest-path code/digimon-engine/Cargo.toml --features dsl-yaml-loader --no-fail-fast` — regression-clean: real test-failure set identical to the pre-change baseline (17 pre-existing; `combat`/`option_flow`/`deletion_batching`/EX10-036 all green). Confirmed twice during Gap 1 implementation + a final combined gate.
