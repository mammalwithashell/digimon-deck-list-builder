cloud_downloads/
__pycache__/
*.pyc
*.egg-info/
/bin
/obj
.gemini/GEMINI.md
Digimon TCG resources/manual.pdf
Digimon TCG resources/general_rule.pdf
Digimon.Shim
Digimon.DCGO
dcgo_build_errors_*
.vscode/settings.json
models/
runs/
.claude/settings.local.json
nul
code/frontend/dist/
code/frontend/node_modules/
.env
.env.local
.env.*.local
data/app.db
code/frontend/tsconfig.tsbuildinfo
.playwright-mcp/
.worktrees/
Digimon TCG resources/glossary.pdf

# Rust / Cargo
# Workspace-level build output (digimon-engine and src-tauri share this target/).
target/
# Rustfmt backup files (written when rustfmt runs with --backup).
**/*.rs.bk
# Perf / profiling output from `cargo flamegraph`, `perf`, etc.
perf.data
perf.data.old
flamegraph.svg
# cargo-expand intermediate files
*.orig

# Tauri
code/src-tauri/target/
code/src-tauri/binaries/
code/src-tauri/resources/models/
code/src-tauri/resources/onnxruntime/
code/tools/onnxruntime-vendor/**/*.dll
code/tools/onnxruntime-vendor/**/*.so
code/tools/onnxruntime-vendor/**/*.dylib
*.AppImage
*.dmg
*.msi
*.deb

# PM Agent
.claude/pm/sessions/
.claude/pm/.briefing-lock
.claude/scheduled_tasks.lock

# digimon-card-lookup: CDN-downloaded image cache for cards missing from the local mirror
.claude/skills/digimon-card-lookup/.cache/

# Generated ONNX test fixtures
code/tests/api/fixtures/*.onnx

# Post-refactor leftovers (real sources moved under code/)
/frontend/
/src-tauri/
/tests/

# Agent worktrees
.claude/worktrees/

# Playwright e2e artifacts
code/frontend/test-results/
code/frontend/playwright-report/

# Per-worktree isolated cargo target scratch dirs (build contention workaround)
.cargo-target-*/

# Local Weights & Biases run artifacts (from local --wandb smoke tests)
wandb/
# Brainstorm visual-companion + screenshot-trial scratch (session-local)
.superpowers/
.trial/
/playwright-launcher.png
