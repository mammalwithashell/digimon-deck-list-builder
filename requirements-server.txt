# Server-only dependencies — no torch/SB3 for smaller Docker image.
# Admin training endpoints (deck_optimizer, /admin/training/*) will 500 if called.
# Set TRAINING_WORKER_DISABLED=1 in prod env; F&F testers never touch these paths.
numpy>=1.24
gymnasium>=0.29
fastapi>=0.100
httpx>=0.24
uvicorn>=0.20
# App config / middleware / logging (all on the server.api startup path)
pydantic-settings>=2.0
slowapi>=0.1.9
structlog>=24.1
platformdirs>=4.0
sentry-sdk[fastapi]>=2.0
# Database & auth
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.19
asyncpg>=0.29
alembic>=1.12
bcrypt>=4.0
python-jose[cryptography]>=3.3
python-multipart>=0.0.6
pydantic[email]>=2.0
Pillow>=10.0
# Meta loader / scraping
beautifulsoup4>=4.12
requests>=2.28
python-dotenv>=1.0
onnxruntime>=1.16
# AI pipeline (admin-AI worker imports these at startup)
openai>=1.40
anthropic>=0.39.0
pypdf>=4.2
rank_bm25>=0.2.2
pinecone[grpc]>=5.0.0
# Spaces (S3-compatible)
boto3
# Optional: DigiLab MotherDuck integration
duckdb>=0.9
