# Rust engine gap-closure plan — ShineGreymon (store-champs June-2026 slice)

## Goal

Close the single blocking gap so the ShineGreymon deck (9 audited missing cards; 8 already
supported) is fully implementable per CLAUDE.md §17–18. Generated by `/assess-archetype-rust`
on 2026-07-02. Scoping: `qa/archetype-qa/store-champs-june-2026-scoping.md`.

## Gap to close

1. **G-DSL-DISTINCT-NAMED-PERMANENT-COUNT** (🔴, blocks BT21-040 clause 2) — predicate leaf
   `distinct_named_count_gte: {of, filter, n}` counting DISTINCT synth-identity-aware card names
   among battle-area permanents matching a sub-filter. Model on `distinct_tamer_colors_gte`'s
   collector + inner filter. Do NOT ship BT21-040 with only the first disjunct (opp Lv6+) — that
   under-approximates a legal digivolve path. NOTE: DCGO under-implements this card (ignores
   "different names"); the printed text governs.

## Implement-now list (SUPPORTED — 8 cards)
BT12-034, BT16-029, BT13-008, BT12-038, BT13-015, BT9-041, BT12-043, BT12-092.

Implementation notes from the audit:
- Idiom exemplars: ST24-07 (free tamer play + DP debuff), BT21-042 (Marcus free-play + per-Tamer aura), ST1-14 (turn-scoped security-Digimon DP player-modifier — flip target_player to opponent for BT12-043), AD1-017/EX7-024 (opponent-security DP aura shapes).
- BT12-092: verify G-EFFECT-INITIATED-DIGIVOLVE-FROM-HAND-WITH-PERMANENT-TARGET is closed (GAPS.md ~line 196 says closed) — its on_suspend clause digivolves a CHOSEN own Digimon from a hand card.
- BT13-008/BT12-092: TreatAsDigimon/SynthIdentity substrate exists (doc-comment cites the Marcus 3000-DP case); "can't digivolve" is naturally satisfied (can_digivolve reads the raw top card). Add a behavioral test for the Q&A edge: Tamer-as-Digimon with stacked cards gains inherited effects.
- BT9-041: the name-in-sources gate matches "RizeGreymon" OR "X Antibody" as source NAMES (not the trait) — Q&A-critical.
- BT16-029 bucket B "2-color card": `self_color_count_gte: 2` is faithful (no card exceeds 2 colors).
- "Also treated as [Marcus Damon]" name-overlay (GAPS.md ~1269) affects BT12-043's aura once dual-named ST24 tamers are in play — fidelity note, not a blocker for these 9.

## References
GAPS.md §"Store-champs June-2026 audit — Millenniummon + ShineGreymon" (the distinct-named-count entry); `code/digimon-dsl/src/predicate.rs` (`distinct_tamer_colors_gte`); tests under `code/digimon-engine/tests/cards_behavioral/{bt12,bt13,bt16,bt21,bt9}/`.

## Ask
Single-phase plan: TDD the predicate leaf (dsl parse test + engine eval test + BT21-040 behavioral), then batch-implement the 9 cards via /batch-implement-cards-rust-dsl.
