# Rust engine gap-closure plan — Three Musketeers BeelStarmon (store-champs June-2026 slice)

## Goal

Close the 3 blocking and 3 partial engine/DSL gaps below so that the Three Musketeers
BeelStarmon deck core (12 missing cards) can be fully implemented in `code/digimon-engine/`
without stubs, approximations, or auto-selections (per CLAUDE.md §17–18).

This prompt was generated by `/assess-archetype-rust` on 2026-07-02. Re-run that skill if card
text, deck list, or engine API has changed. Deck scoping: `qa/archetype-qa/store-champs-june-2026-scoping.md`.

## Gaps to close (ordered by blast radius)

1. **`move_self_option_under_permanent` doesn't compose with the standard Option [Main]-play path** (🔴, blocks 3 cards: P-180, EX7-071, EX7-070) — the in-flight `pending_option` is never claimable for bottom-source placement; mirror the resolved `place_self_as_delay_option` fix. See `docs/RUST_ENGINE_GAPS.md` §"`move_self_option_under_permanent` doesn't compose with the standard Option [Main]-play path".
2. **Option USE routed from non-hand origins + cost delta** (🔴, blocks BT25-083 hard; degrades BT25-085; also blocks Galacticmon BT21-062 and EX7-048) — generalize `play_option_core` entry to Hand/Trash/Revealed/Source origins with `cost: free|printed|{reduce:N}`. See GAPS.md §"Option USE routed from non-hand origins…".
3. **Board-wide leave-field replacement protecting OTHER filtered Digimon, trash-Option-from-carrier-sources cost** (🔴, blocks EX7-048) — extends G-DSL-PROTECT-OTHER-BY-SELF-DELETE with a new ReplacementCostBody. See GAPS.md §"Board-wide leave-field replacement…".
4. **Whole-card "[X] in its text" predicate (`HasText` scope)** (🟡, affects BT25-005, EX7-051, EX7-008, EX7-048 + 8 Galacticmon cards) — new `in_text_contains` leaf; interim `any_of[effect_text_contains, trait_has, name_contains]` workaround acceptable with fidelity note. See GAPS.md §"Whole-card…".
5. **`select_own_sources` excludes link cards** (🟡, degrades BT25-085) — `include_link_cards: true` flag. See GAPS.md §"`select_own_sources` candidate set excludes link cards".
6. **`OnAddDigivolutionCards` trigger timing** (🔴 pre-existing entry, blocks BT25-005 + EX11-074) — already filed 2026-06-06; this archetype re-confirms it. See GAPS.md §"`OnAddDigivolutionCards` trigger timing".

## Cards affected (verbatim list, for regression targeting)

- BT25-005 Pagumon: gaps [OnAddDigivolutionCards (existing), in-text predicate]
- EX7-051 Sparrowmon: gaps [in-text predicate on alt-path; VERIFY hand/trash bottom-source placement via `select_union_zone`+`place_as_bottom_source` (BT25-082 idiom) — one auditor flagged it missing, another verified it present]
- EX7-008 ToyAgumon: gaps [in-text predicate] — otherwise implementable today
- EX7-048 Gundramon: gaps [Option-use from reveal; protect-others replacement w/ Option-trash cost; in-text predicate]
- BT25-083 LadyDevimon: gaps [Option-use from trash with cost −3]
- BT25-085 BeelStarmon: gaps [Option-use from digivolution sources; select_own_sources link cards]
- BT21-074 Satellamon: VERIFY ONLY — the cited "Source-scoped return-immunity" gap was RESOLVED 2026-05-15; likely implementable today
- EX7-073 BeelStarmon (X Antibody): SUPPORTED — implement immediately
- LM-056 Image Training: SUPPORTED — implement immediately
- P-180 / EX7-071 / EX7-070: gaps [move_self_option_under_permanent play-path]

## Relevant references

- `docs/RUST_ENGINE_API.md` — existing scripting surface
- `docs/RUST_ENGINE_GAPS.md` — full gap entries with suggested API shapes (§"Store-champs June-2026 audit")
- `docs/RUST_PYTHON_PARITY.md` — cross-engine divergences
- `code/digimon-engine/src/effect_context/action/lifecycle.rs:222` — `move_self_option_under_permanent`
- `code/digimon-engine/src/game/option_lifecycle.rs:334` — `move_field_option_under_permanent`; the resolved `place_self_as_delay_option_permanent` pending-option claim to mirror
- `code/digimon-engine/src/effect_context/action/play.rs:343,882` — hand vs trash Option dispatch asymmetry
- `code/digimon-engine/src/dsl_cards/predicate.rs` — `effect_text_contains` (in-text scope)
- `code/digimon-engine/src/game/selections.rs:4034` — `has_own_source_candidates` (link-card exclusion)
- `code/digimon-engine/tests/cards_behavioral/ex7/`, `bt25/`, `bt21/` — TDD harness targets (CLAUDE.md §18)

## Ask

Produce a phased implementation plan (per `superpowers:writing-plans`) that:

1. Groups gaps by subsystem: Option lifecycle (gaps 1–2 share `play_option_core` plumbing), replacement framework (gap 3), predicate vocabulary (gaps 4–5), trigger timings (gap 6).
2. Orders phases by dependency (Option-lifecycle plumbing first — it unblocks 5 cards across 2 archetypes) and blast radius.
3. For each phase, lists: exact API surface (signatures/enum variants), tests to write FIRST (name the `code/digimon-engine/tests/*.rs` file and case), affected files, parity implications.
4. Flags architectural items needing a spec under `docs/superpowers/specs/` (the Option-use origin generalization probably qualifies).
5. Prefers 3–6 phases with clear stop points; does NOT close every gap in one phase.
