# Rust engine gap-closure plan — Millenniummon (store-champs June-2026 slice)

## Goal

Close the 5 blocking and 2 partial gaps below so the Millenniummon deck (29 audited cards; 20
already supported) can be fully implemented per CLAUDE.md §17–18. Generated by
`/assess-archetype-rust` on 2026-07-02. Scoping: `qa/archetype-qa/store-champs-june-2026-scoping.md`.

## Gaps to close (ordered by blast radius)

1. **G-ENGINE-COST-REDUCTION-INTERACTIVE-DELETE-COST** (🔴, BT18-073 + BT13-083; also resolves half of BT13-103) — let a `cost_reduction` pay_cost park on a `pending_selection` and resume (via the resumable VM, rule 28 clone-safety). See GAPS.md §Store-champs June-2026 (Millenniummon+ShineGreymon).
2. **G-ENGINE-DNA-TRASH-MATERIAL** (🔴, BT18-015 + BT18-073 On-Deletion DNA) — DNA verb variant with a trash material (materialize-then-merge). Composes with the open G-ENGINE-DNA-PRINTED-COST + G-ENGINE-DNA-RECIPE-ENFORCEMENT (consider closing all three together as one DNA workstream).
3. **G-DSL-COLOR-MATCHES-OWN-SOURCE-STACK** + **G-DSL-DELETE-ONE-PER-DISTINCT-OPPONENT-COLOR** (🔴×2, EX9-074 both branches) — source-stack color-set predicate + per-color mandatory-pick for_each with exclusion.
4. **G-DSL-TRASH-COUNT-RESULT-BINDING** (🔴, BT19-075) — `bind_count_as` on `trash_opponent_hand_to_count`; consumers already exist (`binding_value` + `floor_div`).
5. **G-DSL-COST-RELATIVE-TO-EVENT-SUBJECT** (🔴, BT19-099 Delay rider) — `play_cost_eq_binding {binding, offset}` comparator mirroring `level_eq_binding`.
6. **G-DSL-EVENT-TARGET-SOURCE-COUNT** (🟡, EX1-066) — `event_target_has_digivolution_cards` leaf from the rule-25 deletion snapshot.
7. **G-ENGINE-ON-DISCARD-HAND** (🔴 pre-existing, ST16-14 + BT25-080/029/084) — hand→trash trigger + by-your-own-effect source filter. Biggest cross-set unlock.

## Housekeeping bundled with this plan
- Mark the stale `<Training>` GAPS.md entry (~line 534) resolved (EX9-060 confirms Phase F wiring).
- Reconcile `(Rule) Trait: Has [Composite]` for BT18-073/BT19-065 into `card_overrides.json`.
- BT8-107 needs `level_lte_binding` — verify; trivial widening of `level_eq_binding` if absent.

## Implement-now list (SUPPORTED, no gap dependency — 20 cards)
BT18-019, BT19-101, BT19-070, BT19-065, BT18-013, BT19-069, EX9-060, BT15-006, EX9-058, BT3-077,
EX10-040, BT19-066, BT19-006, EX11-055, P-193, P-205, BT8-107, BT9-070, BT18-007, BT11-018.
P-220 is implementable too (its DNA printed cost is 0 → the printed-cost gap is latent/harmless).
Notes: EX9-058's `bottom_source_of` under a CHOSEN permanent is untested — add an explicit
behavioral test; P-205 — implement per PRINTED text, DCGO's name filter has an operator-precedence
bug; Vemmon-style "in its text" filters are not in this slice.

## References
GAPS.md §"Store-champs June-2026 audit — Millenniummon + ShineGreymon"; `code/digimon-engine/src/dsl_cards/lower_cost_reduction.rs:195`; `code/digimon-engine/src/dna_digivolve.rs` + `game/mod.rs:1895`; `code/digimon-dsl/src/{predicate,formula,step}.rs`; tests under `code/digimon-engine/tests/cards_behavioral/{bt18,bt19,ex9,ex10,ex1,ex11,p,st16,bt15,bt11,bt13,bt9,bt3}/`.

## Ask
Phased plan per `superpowers:writing-plans`: (1) pay_cost park-and-resume, (2) DNA workstream
(trash material + printed cost + recipe enforcement together), (3) predicate/formula leaves
(items 3–6 batched), (4) OnDiscardHand trigger. TDD-first per phase; flag the DNA workstream for
a spec under docs/superpowers/specs/.
