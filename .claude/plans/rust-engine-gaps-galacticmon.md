# Rust engine gap-closure plan — Galacticmon (store-champs June-2026 slice)

## Goal

Close the 5 blocking and 5 partial engine/DSL gaps below so that the Galacticmon deck core
(16 missing cards) can be fully implemented in `code/digimon-engine/` without stubs,
approximations, or auto-selections (per CLAUDE.md §17–18).

Generated by `/assess-archetype-rust` on 2026-07-02. Deck scoping:
`qa/archetype-qa/store-champs-june-2026-scoping.md`.

## Gaps to close (ordered by blast radius)

1. **Source-return-to-deck-bottom observer trigger** (🔴, blocks BT21-058 + BT18-065; the archetype's core engine loop) — new `EffectTiming::OnDigivolutionCardReturnedToDeckBottom` fired from `return_card_source_to_deck`. See `docs/RUST_ENGINE_GAPS.md` §"Source-return-to-deck-bottom observer trigger".
2. **Whole-card "[X] in its text" predicate** (🟡, affects 8 Galacticmon cards + 4 BeelStarmon cards) — `in_text_contains` leaf. Shared with the BeelStarmon plan.
3. **Option USE from non-hand origins** (🔴, blocks BT21-062) — shared with the BeelStarmon plan (§"Option USE routed from non-hand origins").
4. **Leave-field replacement cost: return N own sources to deck bottom** (🔴, blocks BT21-062) — new `ReplacementCostBody` variant; must fire trigger #1. See GAPS.md.
5. **Mass-delete excluding a chosen binding** (🔴, blocks EX11-046) — `is_not_binding` predicate leaf / `over.exclude`. See GAPS.md.
6. **Filtered source-count threshold predicate** (🔴, blocks BT21-006) — `self_source_count {filter, op, value}` comparator. See GAPS.md.
7. **Deck copy-limit raise above format default** (🔴, blocks BT11-061 deck legality) — `deck_tools.rs` override semantics + `max_count_in_deck: 50` data fix. See GAPS.md.
8. **DigiXros conditional trash-origin** (🟡, BT18-065) — predicate-gated `extra_material_zones`. See GAPS.md.
9. **Place-all-revealed-under-event-set + ordering** (🟡, EX11-066) — `place_all_from_reveal_under_target` + `event_permanent_set`. See GAPS.md.
10. **`trash_top_security` to-remaining (`Subtract` formula) + deletion-result binding** (🟡×2, BT21-098). See GAPS.md.

Pre-existing entries reconfirmed (not re-filed): BT15-102 Apocalymon blocked on "Cast-time
stack-construction for cost reduction" (GAPS.md:817) + "Cross-card effect re-firing,
source-card variant" (GAPS.md:833).

## Cards affected (verbatim, for regression targeting)

- BT21-006 Tsumemon: [filtered source-count predicate]
- BT11-061 Vemmon: [deck copy-limit raise] — all behavioral clauses supported
- BT21-056 / BT18-060 Vemmon: SUPPORTED — implement immediately
- BT21-058 Snatchmon: [source-return observer; in-text predicate]
- BT18-065 Snatchmon: [source-return observer; DigiXros conditional trash-origin; EOYT digivolve composition (verify — likely only in-text residual)]
- P-094 Destromon: SUPPORTED (verify `select_own_sources` can bind a foreign [Galacticmon] carrier, not self-only)
- BT21-060 Destromon: SUPPORTED (verify `ImmuneFromStackTrashing` covers top+bottom stacked, opponent-only)
- EX11-046 Galacticmon: [mass-delete exclude binding; in-text predicate]
- BT21-062 Galacticmon: [Option-use from trash/hand-or-trash; replacement cost return-4-sources; in-text predicate]
- EX11-066 Xeno: [place-all-revealed-under-event-set]
- BT18-092 / BT21-087 Zenith: SUPPORTED — implement immediately
- BT21-098 Ragnarok Cannon: [Subtract formula or leave:N; deletion-result binding; in-text predicate]
- BT11-105 Fusionize: SUPPORTED — implement immediately
- BT15-102 Apocalymon: blocked on 2 pre-existing entries (drivers already listed there)

## Implementation notes from the audit

- Vemmon inherited cost-reducers must be host-scoped via `source_is_cost_target_permanent: true`, NOT `when_any_ally_digivolves_into` (over-fires for other allies).
- The in-text workaround `any_of: [name_contains, trait_has, effect_text_contains]` is faithful for the Vemmon family specifically (Vemmon appears in names/text).
- BT18-092's `on_ally_attack` is present in the DSL (`clause.rs:124`); a stale comment in BT21-102's spec claims otherwise — ignore it.

## Relevant references

- `docs/RUST_ENGINE_GAPS.md` §"Store-champs June-2026 audit" — full entries with API shapes
- `code/digimon-engine/src/effect_context/action/sources.rs:891` — `return_card_source_to_deck` (trigger emit site)
- `code/digimon-engine/src/digixros.rs:32,959` — DigiXros material zones
- `code/digimon-engine/src/dsl_cards/{predicate,formula}.rs` — predicate/formula vocabulary
- `code/digimon-engine/src/game/deck_tools.rs:~552` — copy-limit computation
- `code/digimon-dsl/src/clause.rs` — `ReplacementCostBody`, `Timing`
- `code/digimon-engine/tests/cards_behavioral/{bt21,bt18,ex11,bt11,p}/` — TDD targets

## Ask

Produce a phased implementation plan (per `superpowers:writing-plans`) that:

1. Groups by subsystem: trigger timings (#1), replacement framework (#4, depends on #1), Option lifecycle (#3, shared with BeelStarmon plan — implement once), predicate/formula vocabulary (#2, #5, #6, #10), DigiXros (#8), reveal-batch placement (#9), deck tools (#7).
2. Orders by dependency then blast radius; #1 and #2 first (they unblock the most cards).
3. Per phase: exact API surface, tests FIRST (file + case names), affected files, parity implications.
4. Flags architectural items needing a spec (the event-set binding for #9 probably qualifies).
5. Prefers 3–6 phases with stop points.
